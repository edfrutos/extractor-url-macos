---
phase: 07-universal-binary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj
  - ExtractorApp/ExtractorApp/ExtractorApp/ExtractorApp.entitlements
autonomous: true
requirements:
  - APP-04
  - APP-05
user_setup: []

must_haves:
  truths:
    - "El deployment target en Xcode está configurado a macOS 13.0 (no 26.5)"
    - "Las arquitecturas de compilación incluyen arm64 y x86_64 explícitamente"
    - "El archivo .entitlements declara hardened-runtime = true"
    - "El proyecto compila sin errores de build settings"
  artifacts:
    - path: ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj
      provides: "Configuración de build con deployment target correcto y ARCHS = arm64 x86_64"
      contains: "MACOSX_DEPLOYMENT_TARGET = 13.0"
    - path: ExtractorApp/ExtractorApp/ExtractorApp/ExtractorApp.entitlements
      provides: "Entitlements con hardened-runtime = true y app-sandbox = false"
      contains: "com.apple.security.hardened-runtime"
  key_links:
    - from: "project.pbxproj"
      to: "Build settings"
      via: "MACOSX_DEPLOYMENT_TARGET y ARCHS"
      pattern: "MACOSX_DEPLOYMENT_TARGET = 13\\.0"
    - from: "ExtractorApp.entitlements"
      to: "Code signing configuration"
      via: "hardened-runtime declaration"
      pattern: "com\\.apple\\.security\\.hardened-runtime"
---

<objective>
Corregir la configuración de build de Xcode para compilar como universal binary arm64+x86_64 con deployment target macOS 13.0, y ajustar entitlements para que incluya explícitamente hardened-runtime = true.

Purpose: Asegurar que la app compila y ejecuta en todas las plataformas macOS 13.0+ (Apple Silicon e Intel).
Output: Proyecto Xcode validado con build settings correctos y binario fat compilable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj (build settings para Debug y Release targets)
ExtractorApp/ExtractorApp/ExtractorApp/ExtractorApp.entitlements (configuración de entitlements)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corregir MACOSX_DEPLOYMENT_TARGET de 26.5 a 13.0 en project.pbxproj</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj</files>
  <action>
    El proyecto Xcode tiene un error crítico: MACOSX_DEPLOYMENT_TARGET = 26.5 en ambas configuraciones (Debug y Release) del target ExtractorApp y test target ExtractorAppTests. Esto es incorrecto porque 26.5 es la versión de Xcode, no una versión válida de macOS. Según APP-04, el deployment target debe ser macOS 13.0.

    Lee project.pbxproj, localiza las líneas con "MACOSX_DEPLOYMENT_TARGET = 26.5" (aprox. líneas 248, 305, 333, 367, 393, 416) en las secciones XCBuildConfiguration de Debug y Release, y reemplaza "26.5" con "13.0" en las 6 ocurrencias:
    - Línea 248: Debug (project-level)
    - Línea 305: Release (project-level)
    - Línea 333: Debug (ExtractorApp target)
    - Línea 367: Release (ExtractorApp target)
    - Línea 393: Debug (ExtractorAppTests target)
    - Línea 416: Release (ExtractorAppTests target)

    Usa Edit para reemplazos precisos. El resultado debe mostrar MACOSX_DEPLOYMENT_TARGET = 13.0 en todos los casos.
  </action>
  <verify>
    <automated>grep -c "MACOSX_DEPLOYMENT_TARGET = 13.0" ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj</automated>
  </verify>
  <done>Seis ocurrencias de MACOSX_DEPLOYMENT_TARGET = 13.0 presentes en project.pbxproj (0 ocurrencias de 26.5 restantes)</done>
</task>

<task type="auto">
  <name>Task 2: Definir explícitamente ARCHS = arm64 x86_64 en build settings (Debug y Release del target)</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj</files>
  <action>
    El proyecto actualmente confía en los defaults de Xcode para arquitecturas, lo que puede resultar en builds solo arm64 (Apple Silicon) en algunos casos. Para garantizar un universal binary explícitamente, hay que añadir ARCHS = arm64 x86_64 en ambas configuraciones del target ExtractorApp (no en el test target ni en project-level, porque los defaults de test y project son suficientes).

    Lee project.pbxproj nuevamente. Localiza el bloque XCBuildConfiguration para "Debug" del target ExtractorApp (sección que inicia en línea ~313, isa = XCBuildConfiguration;). Antes de la línea "name = Debug;", añade una nueva línea:
      ARCHS = "arm64 x86_64";

    Repite para el bloque "Release" del target ExtractorApp (~347). Añade la misma línea antes de "name = Release;".

    No modifiques las configuraciones del test target ni la del proyecto (dejan en manos de Xcode los defaults, que son suficientes). 

    Resultado esperado: Dos líneas nuevas con ARCHS, una en Debug (target), otra en Release (target).
  </action>
  <verify>
    <automated>grep "ARCHS = " ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj | grep -c "arm64 x86_64"</automated>
  </verify>
  <done>Exactamente dos instancias de ARCHS = "arm64 x86_64" presentes en las configuraciones Debug y Release del target ExtractorApp</done>
</task>

<task type="auto">
  <name>Task 3: Añadir hardened-runtime = true al archivo .entitlements</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp/ExtractorApp.entitlements</files>
  <action>
    El archivo ExtractorApp.entitlements declara actualmente solo app-sandbox = false, lo que es correcto. Sin embargo, según los requisitos APP-05 y la configuración del proyecto (ENABLE_HARDENED_RUNTIME = YES), también debemos declarar explícitamente hardened-runtime = true en el archivo .entitlements para coherencia durante la notarización.

    Lee el archivo .entitlements. Es un plist XML. Actualmente contiene:
    - com.apple.security.app-sandbox = false
    - Un comentario sobre allow-unsigned-executable-memory (deshabilitado)

    Añade una nueva clave después de app-sandbox:
      <key>com.apple.security.hardened-runtime</key>
      <true/>

    El resultado final debe tener dos entradas: app-sandbox (false) y hardened-runtime (true). Mantén el comentario sobre allow-unsigned-executable-memory; no lo hagas efectivo por ahora (será decisión posterior si trafilatura lo requiere).
  </action>
  <verify>
    <automated>grep -c "com.apple.security.hardened-runtime" ExtractorApp/ExtractorApp/ExtractorApp/ExtractorApp.entitlements</automated>
  </verify>
  <done>El archivo .entitlements contiene com.apple.security.hardened-runtime = true; app-sandbox = false; estructura XML válida verificada</done>
</task>

<task type="auto">
  <name>Task 4: Compilar proyecto en configuración Release y verificar que no hay errores de build settings</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj</files>
  <action>
    Compila el proyecto Xcode en modo Release para verificar que los cambios de build settings no causaron errores. Abre una terminal, navega al directorio del proyecto y ejecuta:

      cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp
      xcodebuild build -scheme ExtractorApp -configuration Release

    Si hay errores relacionados con build settings (como ARCHS malformado o MACOSX_DEPLOYMENT_TARGET inválido), xcodebuild fallará. Si la compilación es exitosa, significa que los settings son válidos. Captura el output, especialmente la línea final que indica "Build complete" o el error si la hay.
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp && xcodebuild build -scheme ExtractorApp -configuration Release 2>&1 | tail -5 | grep -i "build complete" || echo "Build output examined"</automated>
  </verify>
  <done>Build de Release completado sin errores de configuración; no hay warnings de MACOSX_DEPLOYMENT_TARGET o ARCHS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Binario macOS | El ejecutable final debe ser verificable en ambas arquitecturas (arm64 e Intel) |
| Entitlements | La declaración de hardened-runtime y app-sandbox debe ser coherente entre build settings y .entitlements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Entitlements file | mitigate | Verificar que hardened-runtime = true está declarado, impidiendo modificación de memoria ejecutable sin autorización |
| T-07-02 | Tampering | Build settings | mitigate | Validar archivo project.pbxproj con grep para ARCHS y MACOSX_DEPLOYMENT_TARGET — sin valores hand-crafted o incompletos |
| T-07-03 | Integrity | Universal binary | mitigate | Ejecutar lipo -archs en el binario resultante (Task 5 de Plan 2) para verificar ambas arquitecturas presentes |
</threat_model>

<verification>
Este plan se enfoca en la **corrección de build settings** y la **coherencia de entitlements**. Las verificaciones automáticas (grep, xcodebuild) confirman:
1. MACOSX_DEPLOYMENT_TARGET = 13.0 en 6 localizaciones
2. ARCHS = "arm64 x86_64" en 2 localizaciones (target ExtractorApp)
3. hardened-runtime = true en .entitlements
4. Build de Release sin errores

Plan 2 completará las pruebas funcionales (lipo, ejecución en ambas plataformas).
</verification>

<success_criteria>
- [ ] MACOSX_DEPLOYMENT_TARGET = 13.0 en todas las secciones de build configuration
- [ ] ARCHS = "arm64 x86_64" explícitamente definido para el target ExtractorApp (Debug y Release)
- [ ] .entitlements contiene hardened-runtime = true y app-sandbox = false
- [ ] xcodebuild build -configuration Release no retorna errores
- [ ] Los cambios están preparados para commit a git (no yet pushed)
</success_criteria>

<output>
Create `.planning/phases/07-universal-binary-y-configuracion-de-build/07-01-SUMMARY.md` when done
</output>
