---
phase: 14-historial-cola
plan: 02
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - ExtractorApp/ExtractorApp/ExtractorApp/Models/HistoryEntry.swift
  - ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/HistoryViewModel.swift
  - ExtractorApp/ExtractorApp/ExtractorApp/Views/HistoryView.swift
  - ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift
autonomous: false
requirements:
  - HIST-02

must_haves:
  truths:
    - "HistoryEntry.loadAll() lee ~/.cache/extractor-url/history.jsonl exactamente en la misma ruta que escribe core.py (Path.home()/.cache/extractor-url/history.jsonl)"
    - "Una línea JSON corrupta en history.jsonl no rompe la carga de las demás entradas"
    - "'Reabrir' una entrada rellena urlString/outputType/selectorCSS de ExtractionViewModel y llama a vm.extract() — no reimplementa el flujo de extracción"
    - "No se escribe ninguna entrada de historial desde Swift — la app solo LEE history.jsonl, quien escribe siempre es el motor Python"
  artifacts:
    - path: "ExtractorApp/ExtractorApp/ExtractorApp/Models/HistoryEntry.swift"
      provides: "HistoryEntry Codable + HistoryEntry.loadAll(limit:)"
      contains: "loadAll"
    - path: "ExtractorApp/ExtractorApp/ExtractorApp/Views/HistoryView.swift"
      provides: "Vista de lista del historial con acción de reabrir por fila"
      contains: "onReopen"
  key_links:
    - from: "ContentView.heroSection"
      to: "HistoryView"
      via: ".sheet(isPresented: $showingHistory) { HistoryView { entry in ... } }"
      pattern: "showingHistory"
    - from: "HistoryView.onReopen"
      to: "ExtractionViewModel.extract()"
      via: "vm.urlString = entry.url; ...; vm.extract()"
      pattern: "vm\\.extract\\(\\)"
---

<objective>
Añadir una vista de historial a ExtractorApp: lee `~/.cache/extractor-url/history.jsonl`
(escrito por el motor Python desde la Fase 14-01) y permite reabrir una
extracción previa rellenando los campos y reejecutando la extracción
(rápida gracias a la caché HTTP de Python). Cierra HIST-02 y, con ello,
la Fase 14 completa.

Purpose: El usuario puede volver a una extracción anterior sin recordar
la URL ni reconfigurar el formato/selector a mano.
Output: 3 archivos Swift nuevos + una adición pequeña a `ContentView.swift`
(botón + sheet), todo escrito en este sandbox — checkpoint humano en
Xcode pendiente (no compilable aquí).
</objective>

<execution_context>
@/Users/edefrutos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-historial-cola/14-02-RESEARCH.md
</context>

<interfaces>
El código de los 3 archivos nuevos y el diff de `ContentView.swift` están
prácticamente completos en `14-02-RESEARCH.md`, sección "Architecture
Patterns" — este plan los traslada tal cual, sin reinterpretación.

`ExtractionViewModel` (ya existente, sin cambios): `urlString`,
`outputType`, `selectorCSS` son `@Published var` de escritura directa;
`extract()` es público y ya evita doble disparo (`guard !isExtracting`).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Models/HistoryEntry.swift + ViewModels/HistoryViewModel.swift</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp/Models/HistoryEntry.swift, ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/HistoryViewModel.swift</files>

  <read_first>
    ExtractorApp/ExtractorApp/ExtractorApp/Models/ExtractionResult.swift — patrón de CodingKeys ya establecido a replicar
    .planning/phases/14-historial-cola/14-02-RESEARCH.md — código citado casi literal
  </read_first>

  <action>
  Crear `Models/HistoryEntry.swift` y `ViewModels/HistoryViewModel.swift`
  exactamente como en el research (sección "Architecture Patterns").
  `HistoryEntry.loadAll()` construye la ruta con
  `FileManager.default.homeDirectoryForCurrentUser.appendingPathComponent(".cache/extractor-url/history.jsonl")`
  — verificar que coincide EXACTAMENTE con `Path.home() / ".cache" / "extractor-url" / "history.jsonl"`
  de `core.py` (mismo `.cache/extractor-url/history.jsonl` relativo al home).
  </action>

  <verify>
    <automated>grep -n "func loadAll\|homeDirectoryForCurrentUser\|class HistoryViewModel" ExtractorApp/ExtractorApp/ExtractorApp/Models/HistoryEntry.swift ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/HistoryViewModel.swift</automated>
  </verify>

  <acceptance_criteria>
    - Ambos archivos existen con el contenido especificado.
    - La ruta construida en `loadAll()` es `.cache/extractor-url/history.jsonl` relativa al home — sin typos frente a la ruta Python.
  </acceptance_criteria>

  <done>HistoryEntry y HistoryViewModel existen, listos para que HistoryView los use.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Views/HistoryView.swift + integración en ContentView.swift</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp/Views/HistoryView.swift, ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift</files>

  <read_first>
    ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift — heroSection (líneas 42-82) y estructura del body (líneas 11-38)
    .planning/phases/14-historial-cola/14-02-RESEARCH.md — sección "Integración en ContentView.swift"
  </read_first>

  <action>
  1. Crear `Views/HistoryView.swift` con `HistoryView` + `HistoryRow` tal
     como en el research.

  2. En `ContentView.swift`:
     - Añadir `@State private var showingHistory: Bool = false` junto a
       las demás `@State` de la struct (línea ~7-9).
     - En `heroSection`, insertar el botón de historial (icono
       `clock.arrow.circlepath`) en el `HStack` principal, ANTES del
       `Spacer()` que ya separa el título del indicador de `isExtracting`
       — no reemplazar nada existente, solo añadir.
     - Adjuntar `.sheet(isPresented: $showingHistory) { HistoryView { entry in ... } }`
       al `ZStack` raíz del `body`, tras el `.onAppear` ya existente (después
       de la línea 37, `}` de cierre del `.onAppear`).
     - El closure de `onReopen` asigna `vm.urlString`, `vm.outputType`
       (con `?? "text"` de fallback) y `vm.selectorCSS`, y llama a
       `vm.extract()` — exactamente como en el research.
  </action>

  <verify>
    <automated>grep -n "showingHistory\|HistoryView\|clock.arrow.circlepath" ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift</automated>
  </verify>

  <acceptance_criteria>
    - `ContentView.swift` compila conceptualmente sin romper la estructura existente (verificación real en el checkpoint humano).
    - El botón de historial está en `heroSection`, no en un sitio que rompa el layout ya existente.
    - `onReopen` no reimplementa lógica de extracción — solo asigna campos y llama a `vm.extract()`.
  </acceptance_criteria>

  <done>HistoryView existe, ContentView tiene el botón + sheet, listo para el checkpoint humano en Xcode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `history.jsonl` (escrito por Python) → `HistoryEntry.loadAll()` (leído por Swift) | Archivo local del propio usuario, mismo nivel de confianza que cualquier archivo de caché ya leído por la app — sin datos de terceros ni red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | Línea JSON corrupta en `history.jsonl` | mitigate | `try?` por línea al decodificar — una línea corrupta se ignora sin romper la carga de las demás, mismo principio que `load_history()` en Python. |
| T-14-02-SC | Tampering | npm/pip/cargo installs | accept | Este plan no añade dependencias — solo Foundation (`JSONDecoder`, `FileManager`) ya usado en el resto del proyecto. |
</threat_model>

<verification>
No ejecutable en este sandbox (sin Xcode). Verificación estructural:

```bash
cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url
grep -n "func loadAll\|class HistoryViewModel\|struct HistoryView\|showingHistory" \
  ExtractorApp/ExtractorApp/ExtractorApp/Models/HistoryEntry.swift \
  ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/HistoryViewModel.swift \
  ExtractorApp/ExtractorApp/ExtractorApp/Views/HistoryView.swift \
  ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift
```

La verificación real (compila, muestra entradas reales, reabrir funciona)
es el checkpoint humano — ver
`.planning/phases/14-historial-cola/CHECKPOINT-HUMANO.md`.
</verification>

<success_criteria>
1. `HistoryEntry.loadAll()` lee `~/.cache/extractor-url/history.jsonl` en la misma ruta exacta que escribe `core.py`.
2. Una línea corrupta no rompe la carga de las demás entradas.
3. El botón de historial abre un sheet con las entradas, más reciente primero.
4. Reabrir una entrada rellena los campos y reextrae vía el flujo ya existente — sin lógica de extracción duplicada.
5. El checkpoint humano confirma: Build Succeeded, el historial muestra extracciones reales hechas desde la propia app, y reabrir funciona de principio a fin.
</success_criteria>

<output>
Crear `.planning/phases/14-historial-cola/14-02-SUMMARY.md` cuando el checkpoint humano confirme Build Succeeded y el flujo completo verificado.
</output>
