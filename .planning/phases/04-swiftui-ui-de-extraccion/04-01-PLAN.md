---
phase: 04-swiftui-ui-de-extraccion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift
autonomous: true
requirements: [APP-01, APP-02, APP-03, UI-01, UI-03]
must_haves:
  truths:
    - "ExtractionViewModel compila como ObservableObject con @Published properties según D-06"
    - "extract() limpia errorMessage y resultContent antes de lanzar (D-09)"
    - "extract() usa Task.detached + MainActor.run, nunca bloquea el hilo principal"
    - "Botón Extraer se puede deshabilitar con vm.isExtracting || vm.urlString.isEmpty (D-07)"
    - "El caso pythonNotFound produce el hint 'Configura la ruta en Preferencias (⌘,)'"
    - "selector vacío se pasa como nil a PythonBridge (no como string vacío)"
    - "timeout == 0 o nil del formatter cae al valor por defecto 15 (D-12)"
  artifacts:
    - path: "ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift"
      provides: "ViewModel completo con todos los @Published y extract()"
      exports: ["ExtractionViewModel"]
  key_links:
    - from: "ExtractionViewModel.extract()"
      to: "PythonBridge.run(url:outputType:selector:timeout:)"
      via: "Task.detached(priority: .userInitiated)"
      pattern: "Task\\.detached"
    - from: "ExtractionViewModel"
      to: "MainActor.run"
      via: "await MainActor.run { self?.isExtracting = false }"
      pattern: "MainActor\\.run"
---

<objective>
Crear ExtractionViewModel — la clase ObservableObject que gestiona todo el estado reactivo
de la pantalla de extracción y orquesta la llamada async a PythonBridge.

Purpose: Separar la lógica de negocio de la vista. La View del plan 04-02 consume este
ViewModel sin saber nada de PythonBridge, Task ni MainActor.

Output: ViewModels/ExtractionViewModel.swift compilable, sin vista que lo use aún.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Contratos que ExtractionViewModel usa directamente -->
</context>

<interfaces>
<!-- Contratos extraídos del código existente. El ejecutor los usa sin exploración adicional. -->

De ExtractorApp/ExtractorApp/ExtractorApp/Services/PythonBridge.swift:
```swift
final class PythonBridge {
    func run(
        url: String,
        outputType: String,
        selector: String? = nil,
        timeout: Int = 15
    ) async throws -> ExtractionResult
    // Lee @AppStorage("pythonPath") y @AppStorage("scriptPath") internamente.
    // NUNCA modificar este archivo.
}
```

De ExtractorApp/ExtractorApp/ExtractorApp/Models/ExtractionResult.swift:
```swift
struct ExtractionResult: Codable {
    let content: String?
    let isSuccess: Bool        // computed: status == "success"
    let errorMessage: String?
    // también: status, url, selector, outputType, charCount
}
```

De ExtractorApp/ExtractorApp/ExtractorApp/Models/ExtractionError.swift:
```swift
enum ExtractionError: Error, LocalizedError {
    case pythonNotFound(path: String)   // → hint "Configura la ruta en Preferencias (⌘,)"
    case processLaunchFailed(underlying: Error)
    case extractionFailed(message: String)
    case jsonDecodeFailed(underlying: Error)
    case emptyOutput
    // var errorDescription: String? implementado en cada caso
}
```

Patrón async establecido en ContentView.swift (BridgeTestViewModel — a eliminar en plan 04-02):
```swift
Task.detached(priority: .userInitiated) { [weak self] in
    do {
        let result = try await self?.bridge.run(...)
        await MainActor.run {
            self?.resultContent = result?.content
            self?.isExtracting = false
        }
    } catch {
        await MainActor.run {
            self?.errorMessage = error.localizedDescription
            self?.isExtracting = false
        }
    }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Crear carpeta ViewModels y escribir ExtractionViewModel.swift</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift</files>
  <action>
Crear la carpeta ViewModels/ dentro de ExtractorApp/ExtractorApp/ExtractorApp/ si no existe,
y escribir ExtractionViewModel.swift con el siguiente contenido exacto.

REQUISITOS CRÍTICOS antes de escribir:
- D-04: usar @StateObject + ObservableObject, NO @Observable macro
- D-06: exponer exactamente urlString, outputType, selectorCSS, timeout, isExtracting, resultContent, errorMessage
- D-07: la lógica de disable se basa en isExtracting y urlString.isEmpty — extraer() no lanza si ya está extrayendo
- D-09: limpiar errorMessage = nil y resultContent = nil ANTES de lanzar la extracción
- D-12: timeout con valor por defecto 15; si NumberFormatter devuelve 0 usar 15
- Selector vacío → pasar nil a PythonBridge (no pasar string vacío)
- pythonNotFound → añadir hint específico al errorMessage

Contenido exacto del archivo:

```swift
import Foundation
import SwiftUI

@MainActor
final class ExtractionViewModel: ObservableObject {

    // MARK: - Inputs (D-06)
    @Published var urlString: String = ""
    @Published var outputType: String = "text"   // "text" | "html" | "markdown"
    @Published var selectorCSS: String = ""
    @Published var timeout: Int = 15             // D-12: valor por defecto 15

    // MARK: - Outputs (D-06)
    @Published var isExtracting: Bool = false
    @Published var resultContent: String? = nil
    @Published var errorMessage: String? = nil
    @Published var isPythonPathError: Bool = false  // hint "Preferencias" en la View

    // MARK: - Private
    private let bridge = PythonBridge()

    // MARK: - Actions

    /// Lanza la extracción. No relanza si ya hay una en curso (D-07).
    func extract() {
        guard !isExtracting, !urlString.isEmpty else { return }

        // D-09: limpiar estado anterior antes de lanzar
        errorMessage = nil
        resultContent = nil
        isPythonPathError = false
        isExtracting = true

        let url = urlString
        let type = outputType
        let selector: String? = selectorCSS.trimmingCharacters(in: .whitespaces).isEmpty
            ? nil
            : selectorCSS.trimmingCharacters(in: .whitespaces)
        let timeoutValue = timeout > 0 ? timeout : 15  // D-12: fallback a 15

        Task.detached(priority: .userInitiated) { [weak self] in
            do {
                let result = try await self?.bridge.run(
                    url: url,
                    outputType: type,
                    selector: selector,
                    timeout: timeoutValue
                )
                await MainActor.run {
                    self?.resultContent = result?.content
                    self?.isExtracting = false
                }
            } catch {
                await MainActor.run {
                    self?.handleError(error)
                    self?.isExtracting = false
                }
            }
        }
    }

    // MARK: - Error formatting

    private func handleError(_ error: Error) {
        errorMessage = error.localizedDescription
        if let e = error as? ExtractionError, case .pythonNotFound = e {
            isPythonPathError = true
        }
    }
}
```

NOTAS DE IMPLEMENTACIÓN:
- La clase está marcada @MainActor para que todas las propiedades @Published se actualicen
  en el hilo principal sin necesidad de MainActor.run explícito en cada assign. El
  Task.detached sigue siendo necesario para que el trabajo async no bloquee el actor
  principal durante la llamada al bridge.
- No importar Combine — @Published no lo requiere en SwiftUI desde Swift 5.7+.
- No crear init() explícito — los valores por defecto en las propiedades son suficientes.
- Añadir el archivo al target de Xcode: en Xcode, File → Add Files to "ExtractorApp" →
  seleccionar ViewModels/ExtractionViewModel.swift → confirmar que el target ExtractorApp
  está marcado.
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && grep -c "@Published" ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift</automated>
  </verify>
  <done>
El archivo existe, contiene exactamente 7 propiedades @Published (urlString, outputType,
selectorCSS, timeout, isExtracting, resultContent, errorMessage), y el método extract()
usa Task.detached con el patrón [weak self]. Comprobación: grep "@Published" devuelve 7.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Añadir el archivo al proyecto Xcode y verificar compilación</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj</files>
  <behavior>
    - El archivo ExtractionViewModel.swift aparece referenciado en el .pbxproj
    - xcodebuild compila sin errores ni warnings de tipo "Cannot find type"
    - BridgeTestViewModel NO aparece en ningún archivo del target (se elimina en 04-02,
      pero en este plan su presencia en ContentView no provoca error de compilación aún)
  </behavior>
  <action>
Añadir ViewModels/ExtractionViewModel.swift al target de Xcode usando ruby/xcodeproj o
editando el .pbxproj directamente si xcodeproj no está disponible.

OPCIÓN PREFERIDA — usar xcodeproj gem si está instalado:
```bash
gem list xcodeproj 2>/dev/null | grep xcodeproj
```

Si xcodeproj está disponible, ejecutar este script Ruby desde la raíz del repo:
```bash
ruby -e "
require 'xcodeproj'
project_path = 'ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj'
proj = Xcodeproj::Project.open(project_path)
target = proj.targets.find { |t| t.name == 'ExtractorApp' }
group = proj.main_group['ExtractorApp']['ExtractorApp']
vm_group = group['ViewModels'] || group.new_group('ViewModels', 'ViewModels')
file_ref = vm_group.new_file('ViewModels/ExtractionViewModel.swift')
target.add_file_references([file_ref])
proj.save
puts 'OK: ExtractionViewModel.swift añadido al target ExtractorApp'
"
```

Si xcodeproj NO está disponible (gem no instalado), abrir Xcode manualmente:
1. En el Project Navigator, click derecho sobre el grupo ExtractorApp → Add Files to "ExtractorApp"
2. Navegar a ViewModels/ExtractionViewModel.swift
3. Asegurar que "Add to targets: ExtractorApp" está marcado → Add

Después de añadir por cualquier método, compilar para verificar:
```bash
xcodebuild \
  -project ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj \
  -scheme ExtractorApp \
  -destination 'platform=macOS' \
  -configuration Debug \
  build 2>&1 | tail -20
```

Si la compilación falla con "Cannot find type 'ExtractionViewModel'", el archivo no fue
añadido al target — repetir el paso de añadir.

Si la compilación falla con cualquier otro error relacionado con ExtractionViewModel.swift,
corregir el error en el archivo antes de continuar con 04-02-PLAN.md.

IMPORTANTE: ContentView.swift aún referencia BridgeTestViewModel — esto es correcto en
este plan. No modificar ContentView.swift aquí; se reescribe completamente en 04-02.
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && xcodebuild -project ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj -scheme ExtractorApp -destination 'platform=macOS' -configuration Debug build 2>&1 | grep -E "BUILD (SUCCEEDED|FAILED)"</automated>
  </verify>
  <done>
xcodebuild reporta BUILD SUCCEEDED. El archivo ExtractionViewModel.swift está en el
target y el proyecto compila limpio.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| ViewModel → PythonBridge | El ViewModel pasa urlString sin sanitizar; PythonBridge lo pasa al CLI de Python como argumento posicional |
| Task.detached → MainActor | Actualizaciones de @Published deben ocurrir en MainActor; el patrón [weak self] previene retención |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-01 | Tampering | urlString sin validar | accept | La URL se pasa como argumento a Process(), no a shell. Sin interpolación shell. La validación de URL bien formada es UX (Fase 5), no seguridad aquí. |
| T-04-02 | Denial of Service | timeout = 0 / negativo | mitigate | `let timeoutValue = timeout > 0 ? timeout : 15` en extract() garantiza mínimo 1 s efectivo vía fallback. |
| T-04-03 | Information Disclosure | errorMessage expone paths del sistema | accept | El mensaje de error es solo visible para el usuario local de la app de escritorio. No hay transmisión de red. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes. Sin riesgo de supply chain en este plan. |
</threat_model>

<verification>
Verificación end-to-end del plan 04-01:

1. El archivo existe en la ruta correcta:
   `ls ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift`

2. Contiene exactamente 7 propiedades @Published:
   `grep -c "@Published" ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift`
   → debe devolver 7

3. No usa @Observable macro:
   `grep "@Observable" ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift`
   → no debe devolver resultados

4. Usa Task.detached (patrón correcto):
   `grep "Task.detached" ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift`
   → debe devolver 1 resultado

5. Limpia estado antes de extraer (D-09):
   `grep "errorMessage = nil" ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift`
   → debe devolver al menos 1 resultado

6. Compilación limpia:
   `xcodebuild -project ... build 2>&1 | grep "BUILD SUCCEEDED"`
</verification>

<success_criteria>
- ExtractionViewModel.swift existe en ViewModels/ y está añadido al target Xcode
- 7 propiedades @Published con los nombres exactos de D-06
- extract() implementa D-07 (guard !isExtracting), D-09 (limpiar antes), D-12 (fallback timeout)
- Selector vacío → nil a PythonBridge
- pythonNotFound produce mensaje + hint "Configura la ruta en Preferencias (⌘,)"
- xcodebuild BUILD SUCCEEDED con ContentView.swift aún en su estado de andamio de Fase 3
</success_criteria>

<output>
Crear `.planning/phases/04-swiftui-ui-de-extraccion/04-01-SUMMARY.md` cuando el plan esté completo.
</output>
