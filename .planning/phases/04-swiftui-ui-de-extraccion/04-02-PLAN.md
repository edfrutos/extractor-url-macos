---
phase: 04-swiftui-ui-de-extraccion
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift
autonomous: true
requirements: [APP-01, APP-02, APP-03, UI-01, UI-03]
must_haves:
  truths:
    - "La ventana muestra campo URL, fila Picker+Botón, DisclosureGroup, área resultado/error (D-01)"
    - "Área de resultado es ScrollView { Text } con fuente monoespaciada (D-02)"
    - "Ventana tiene frame minWidth:500 minHeight:450 (D-03)"
    - "El ViewModel es @StateObject private var vm = ExtractionViewModel() (D-04, D-05)"
    - "Botón Extraer tiene .disabled(vm.isExtracting || vm.urlString.isEmpty) (D-07)"
    - "Error inline con .foregroundColor(.red) y .font(.caption) sin alert modal (D-08)"
    - "ScrollView muestra solo result.content sin cabecera de metadatos (D-10)"
    - "DisclosureGroup arranca isExpanded = false (D-13)"
    - "Strings exactos según UI-SPEC: placeholder URL, Label botón, ProgressView, Picker opciones"
    - "BridgeTestViewModel eliminado del archivo"
    - "xcodebuild BUILD SUCCEEDED"
  artifacts:
    - path: "ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift"
      provides: "Vista principal de extracción completa"
      contains: "ExtractionViewModel"
  key_links:
    - from: "ContentView"
      to: "ExtractionViewModel"
      via: "@StateObject private var vm = ExtractionViewModel()"
      pattern: "@StateObject"
    - from: "Button Extraer"
      to: "vm.extract()"
      via: "action closure"
      pattern: "vm\\.extract()"
    - from: "TextField URL"
      to: "vm.urlString"
      via: "$vm.urlString binding"
      pattern: "\\$vm\\.urlString"
---

<objective>
Reescribir ContentView.swift completamente — eliminar BridgeTestViewModel y el andamio de
Fase 3, e implementar la interfaz de extracción real siguiendo al pie de la letra el
contrato de UI-SPEC y las decisiones D-01 a D-13.

Purpose: Primera interfaz funcional end-to-end. El usuario puede introducir una URL,
configurar opciones, lanzar la extracción y ver el resultado o el error directamente
en la ventana principal.

Output: ContentView.swift listo para producción que consume ExtractionViewModel (plan 04-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-swiftui-ui-de-extraccion/04-UI-SPEC.md
</context>

<interfaces>
<!-- Contrato de ExtractionViewModel creado en plan 04-01 -->

De ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift:
```swift
@MainActor
final class ExtractionViewModel: ObservableObject {
    @Published var urlString: String = ""
    @Published var outputType: String = "text"      // "text" | "html" | "markdown"
    @Published var selectorCSS: String = ""
    @Published var timeout: Int = 15

    @Published var isExtracting: Bool = false
    @Published var resultContent: String? = nil
    @Published var errorMessage: String? = nil
    @Published var isPythonPathError: Bool = false  // true solo si error es pythonNotFound

    func extract()   // lanza extracción async; limpia estado antes de lanzar
}
```

Strings exactos del UI-SPEC (copiar literalmente, sin parafrasear):
- Placeholder URL: "https://example.com"
- Botón: Label("Extraer", systemImage: "arrow.down.circle")
- accessibilityLabel del botón: "Extraer contenido de la URL"
- ProgressView: "Extrayendo…"  ← U+2026, NO tres puntos ASCII (...)
- Picker opciones: "Texto", "HTML", "Markdown"  ← con mayúscula inicial
- DisclosureGroup: "Opciones avanzadas"
- Label CSS: "Selector CSS"  |  Placeholder CSS: "article, .content…"  ← U+2026
- Label timeout: "Tiempo límite (segundos)"  |  Placeholder timeout: "15"
- Error: "Error: \(vm.errorMessage!)"
- Hint pythonNotFound: "Configura la ruta en Preferencias (⌘,)"  (incluido en errorMessage por el ViewModel)

Estados de ventana (del UI-SPEC):
1. Inicial: urlString vacío → botón disabled, área vacía
2. URL introducida: botón enabled, Picker activo
3. Extrayendo: ProgressView visible, fields disabled, resultContent/errorMessage nil
4. Éxito: ScrollView con contenido, sin área de error
5. Error: área resultado vacía, Text error visible en rojo
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reescribir ContentView.swift con la interfaz de extracción completa</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift</files>
  <action>
Reemplazar el contenido completo de ContentView.swift. Eliminar BridgeTestViewModel y
toda referencia al andamio de Fase 3. Escribir la vista siguiendo exactamente D-01 a D-13
y los strings del UI-SPEC.

Contenido exacto del archivo:

```swift
import SwiftUI

struct ContentView: View {
    @StateObject private var vm = ExtractionViewModel()  // D-04, D-05
    @State private var isExpanded: Bool = false          // D-13: colapsado por defecto

    var body: some View {
        VStack(spacing: 16) {  // D-01: VStack lineal

            // ── Campo URL ──────────────────────────────────────────────
            TextField("https://example.com", text: $vm.urlString)  // UI-SPEC placeholder
                .textContentType(.URL)
                .onSubmit { vm.extract() }
                .disabled(vm.isExtracting)

            Divider()  // entre URL y fila Picker+Botón

            // ── Fila Picker de tipo + Botón Extraer ────────────────────
            HStack(spacing: 8) {
                Picker("Tipo", selection: $vm.outputType) {
                    Text("Texto").tag("text")         // UI-SPEC: "Texto"
                    Text("HTML").tag("html")          // UI-SPEC: "HTML"
                    Text("Markdown").tag("markdown")  // UI-SPEC: "Markdown"
                }
                .pickerStyle(.segmented)              // Claude's Discretion: segmented
                .disabled(vm.isExtracting)

                Button {
                    vm.extract()
                } label: {
                    Label("Extraer", systemImage: "arrow.down.circle")  // UI-SPEC exacto
                }
                .buttonStyle(.borderedProminent)                          // UI-SPEC: acento exclusivo
                .accessibilityLabel("Extraer contenido de la URL")       // UI-SPEC exacto
                .disabled(vm.isExtracting || vm.urlString.isEmpty)       // D-07
            }

            Divider()  // entre fila Picker+Botón y DisclosureGroup

            // ── Opciones avanzadas ─────────────────────────────────────
            DisclosureGroup("Opciones avanzadas", isExpanded: $isExpanded) {  // D-11
                VStack(spacing: 8) {
                    HStack {
                        Text("Selector CSS")          // UI-SPEC label exacto
                            .frame(width: 160, alignment: .leading)
                        TextField("article, .content\u{2026}", text: $vm.selectorCSS)  // U+2026
                            .disabled(vm.isExtracting)
                    }
                    HStack {
                        Text("Tiempo límite (segundos)")  // UI-SPEC label exacto
                            .frame(width: 160, alignment: .leading)
                        TextField("15", value: $vm.timeout, formatter: NumberFormatter())  // D-12
                            .disabled(vm.isExtracting)
                    }
                }
                .padding(.top, 4)
            }
            .disabled(vm.isExtracting)

            Divider()  // entre DisclosureGroup y área resultado

            // ── Área resultado / progreso / error ──────────────────────
            // D-01: area final del VStack
            Group {
                if vm.isExtracting {
                    // Estado 3: extrayendo — APP-02
                    ProgressView("Extrayendo\u{2026}")  // U+2026, UI-SPEC exacto
                } else if let content = vm.resultContent {
                    // Estado 4: éxito — D-02, D-10
                    ScrollView {
                        Text(content)  // solo content, sin metadatos (D-10)
                            .font(.system(.caption, design: .monospaced))  // D-02
                            .padding()
                            .frame(maxWidth: .infinity, alignment: .leading)
                    }
                } else if let errorMsg = vm.errorMessage {
                    // Estado 5: error — D-08, APP-03
                    VStack(alignment: .leading, spacing: 4) {
                        Text("Error: \(errorMsg)")
                            .foregroundColor(.red)
                            .font(.caption)
                        if vm.isPythonPathError {
                            // UI-SPEC: hint en .secondary, visualmente diferenciado
                            Text("Configura la ruta en Preferencias (⌘,)")
                                .font(.caption)
                                .foregroundColor(.secondary)
                        }
                    }
                    .frame(maxWidth: .infinity, alignment: .leading)
                }
                // Estado 1/2: vacío — sin contenido visible
            }
            .frame(maxWidth: .infinity, maxHeight: .infinity)

        }
        .padding()
        .frame(minWidth: 500, minHeight: 450)  // D-03
    }
}

// MARK: - Preview
#Preview {
    ContentView()
}
```

REGLAS DE IMPLEMENTACIÓN:
- NO importar Combine — no se usa directamente en la View.
- BridgeTestViewModel se elimina por completo de este archivo (D-05).
- Los strings "Extrayendo…" y "article, .content…" usan el carácter Unicode U+2026
  (HORIZONTAL ELLIPSIS), no tres puntos ASCII. En Swift literal: "\u{2026}".
- El hint de pythonNotFound se incluye en vm.errorMessage (hecho por el ViewModel en 04-01);
  la View NO duplica la lógica de detección del tipo de error.
- El Picker usa .tag("text"), .tag("html"), .tag("markdown") para que coincida con los
  valores que PythonBridge pasa al CLI con --type.
- UI-03 (controles export deshabilitados hasta extracción completa) se satisface con
  .disabled(vm.isExtracting) en los campos de entrada. Los controles de exportación
  propiamente dichos (botones Export) pertenecen a Fase 5; en esta fase el requisito
  se satisface con el hecho de que resultContent == nil mientras isExtracting == true.
  </action>
  <verify>
    <automated>grep -c "@StateObject" /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift</automated>
  </verify>
  <done>
ContentView.swift contiene @StateObject (no BridgeTestViewModel), tiene el frame D-03,
el ProgressView con "Extrayendo…" (U+2026), el Picker segmented, el DisclosureGroup
con isExpanded=false, y el error inline en .red. grep "BridgeTestViewModel" no devuelve
ningún resultado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar compilación end-to-end y cinco estados de ventana</name>
  <files></files>
  <action>
Este task no modifica archivos. Verifica que el proyecto compila limpio y que la
estructura de estados está correctamente implementada en el código.

PASO 1 — Compilar con xcodebuild:
```bash
xcodebuild \
  -project ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj \
  -scheme ExtractorApp \
  -destination 'platform=macOS' \
  -configuration Debug \
  build 2>&1 | tail -30
```

El resultado debe terminar con "BUILD SUCCEEDED".

Si hay errores de compilación, los casos más probables y sus soluciones:

a) "Cannot find type 'ExtractionViewModel'" → ExtractionViewModel.swift no está añadido
   al target. Completar Task 2 del plan 04-01 primero.

b) "Value of type 'ExtractionViewModel' has no member 'isRunning'" → El código usa
   nombres del BridgeTestViewModel antiguo. Revisar que ContentView.swift usa las
   propiedades de D-06 (isExtracting, resultContent, errorMessage, urlString).

c) "Cannot use instance member 'vm' within property initializer" → Asegurarse de que
   @StateObject usa ExtractionViewModel(), no una variable capturada.

d) "Argument type 'Binding<Int>' cannot be converted to 'Binding<String>'" en el
   TextField de timeout → Confirmar que vm.timeout es @Published var timeout: Int
   y el TextField usa value: $vm.timeout (no text:).

PASO 2 — Verificar estructura de estados con grep:
```bash
# Estado 3: ProgressView
grep "ProgressView" ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift

# Estado 4: ScrollView resultado (D-02)
grep "resultContent" ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift

# Estado 5: error inline (D-08)
grep "foregroundColor(.red)" ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift

# D-03: frame correcto
grep "minWidth: 500" ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift

# D-07: disable correcto
grep "vm.isExtracting || vm.urlString.isEmpty" ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift

# BridgeTestViewModel eliminado (D-05)
grep "BridgeTestViewModel" ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift
# → debe devolver 0 resultados
```

PASO 3 — Verificar que no quedan referencias a BridgeTestViewModel en ningún archivo:
```bash
grep -r "BridgeTestViewModel" ExtractorApp/ExtractorApp/ExtractorApp/ --include="*.swift"
# → debe devolver 0 resultados
```

PASO 4 — Smoke test manual (opcional pero recomendado):
Abrir la app en Xcode → Product → Run (⌘R). Sin configurar rutas en Preferencias:
- El campo URL debe aparecer vacío con placeholder "https://example.com"
- El botón Extraer debe estar disabled
- Escribir cualquier URL → el botón debe habilitarse
- Pulsar Extraer → debe aparecer ProgressView y luego el error pythonNotFound en rojo
  con el hint "Configura la ruta en Preferencias (⌘,)"
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && xcodebuild -project ExtractorApp/ExtractorApp/ExtractorApp.xcodeproj -scheme ExtractorApp -destination 'platform=macOS' -configuration Debug build 2>&1 | grep -E "BUILD (SUCCEEDED|FAILED)"</automated>
  </verify>
  <done>
xcodebuild reporta BUILD SUCCEEDED. grep "BridgeTestViewModel" en todos los .swift del
target devuelve 0 resultados. Los cinco estados de ventana están representados en el
código de ContentView.swift (TextField URL, ProgressView, ScrollView resultado,
Text error, y ausencia de contenido para estado inicial/URL introducida).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| Usuario → TextField URL | Entrada libre del usuario; se pasa al ViewModel sin validación de formato URL |
| ContentView → ExtractionViewModel | Binding directo; la View puede leer errorMessage que puede contener paths del sistema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-04-04 | Information Disclosure | errorMessage en UI | accept | La app es local de escritorio. El usuario es el dueño del sistema; ver paths propios es esperado. |
| T-04-05 | Spoofing | TextField URL acepta cualquier string | accept | La URL se valida implícitamente cuando el proceso Python intenta conectarse. Error de red tipado en ExtractionError.extractionFailed. |
| T-04-06 | Denial of Service | Usuario puede disparar múltiples extracciones | mitigate | D-07: .disabled(vm.isExtracting) en el Botón Extraer. Guard en extract() previene llamadas paralelas. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes. Sin riesgo de supply chain. |
</threat_model>

<verification>
Verificación completa del plan 04-02:

1. ContentView.swift contiene ExtractionViewModel (no BridgeTestViewModel):
   `grep "@StateObject private var vm = ExtractionViewModel" ExtractorApp/...ContentView.swift`

2. Frame correcto (D-03):
   `grep "minWidth: 500, minHeight: 450" ExtractorApp/...ContentView.swift`

3. ProgressView con el string correcto:
   `grep "Extrayendo" ExtractorApp/...ContentView.swift`
   → debe aparecer con U+2026, no "..."

4. Error inline en rojo (D-08):
   `grep "foregroundColor(.red)" ExtractorApp/...ContentView.swift`

5. BridgeTestViewModel eliminado (D-05):
   `grep -r "BridgeTestViewModel" ExtractorApp/ExtractorApp/ExtractorApp/ --include="*.swift"`
   → 0 resultados

6. Compilación limpia:
   `xcodebuild ... build 2>&1 | grep "BUILD SUCCEEDED"`

7. @Observable NO presente (D-04):
   `grep "@Observable" ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift`
   → 0 resultados
</verification>

<success_criteria>
- ContentView.swift reescrito sin rastro de BridgeTestViewModel
- Los 5 estados de ventana están implementados: inicial, URL-introducida, extrayendo, éxito, error
- Todos los strings coinciden exactamente con UI-SPEC (U+2026, mayúsculas de Picker, labels)
- frame(minWidth: 500, minHeight: 450) presente (D-03)
- @StateObject + ObservableObject, sin @Observable macro (D-04)
- xcodebuild BUILD SUCCEEDED limpio
- La app arranca y muestra el campo URL con placeholder correcto
- Al pulsar Extraer sin rutas configuradas aparece el error pythonNotFound con hint de Preferencias
</success_criteria>

<output>
Crear `.planning/phases/04-swiftui-ui-de-extraccion/04-02-SUMMARY.md` cuando el plan esté completo.
</output>
