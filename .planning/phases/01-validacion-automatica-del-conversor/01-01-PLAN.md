---
phase: 01-validacion-automatica-del-conversor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extractor_url.py
  - CLAUDE.md
  - NOTEBOOK.md
  - tests/test_converter.py
  - tests/fixtures/edefrutos_me.html
  - tests/fixtures/sample_selector.html
autonomous: true
requirements: [REQ-01, REQ-02, REQ-03]
user_setup: []
must_haves:
  truths:
    - El proyecto puede ejecutar `pytest tests/` sin depender de sitios web reales.
    - La conversión Markdown queda validada para limpieza DOM, selector CSS y URLs relativas.
    - La documentación principal describe el estado real de la implementación actual.
  artifacts:
    - tests/test_converter.py
    - tests/fixtures/edefrutos_me.html
    - tests/fixtures/sample_selector.html
    - CLAUDE.md
    - NOTEBOOK.md
  key_links:
    - tests/test_converter.py verifica funciones y flujos reales de extractor_url.py.
    - CLAUDE.md y NOTEBOOK.md quedan alineados con la implementación actual.
---

<objective>
Crear la base ejecutable de validación automática para el conversor actual y dejar la documentación técnica principal alineada con ese estado.

Purpose: Reducir el riesgo de regresiones antes de abordar nuevas mejoras del extractor.
Output: Fixtures HTML locales, suite inicial de `pytest` y documentación actualizada.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@extractor_url.py
@CLAUDE.md
@NOTEBOOK.md
@pruebas/edefrutos_me.md
@pruebas/edefrutos_me_v2.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Crear fixtures y pruebas base del conversor</name>
  <files>tests/test_converter.py, tests/fixtures/edefrutos_me.html, tests/fixtures/sample_selector.html</files>
  <read_first>extractor_url.py, NOTEBOOK.md, pruebas/edefrutos_me.md, pruebas/edefrutos_me_v2.md</read_first>
  <action>Crea el directorio `tests/` con fixtures HTML locales y añade una suite inicial de `pytest` que valide `_clean_soup()`, `_main_content()`, `_post_process_markdown()` y el flujo de Markdown con selector o HTML controlado. Usa mocks o HTML estático; no hagas peticiones reales de red.</action>
  <verify>python -m pytest tests/ -q</verify>
  <acceptance_criteria>
    - `tests/test_converter.py` existe
    - `tests/fixtures/edefrutos_me.html` existe
    - `tests/fixtures/sample_selector.html` existe
    - `tests/test_converter.py` contiene `def test_`
  </acceptance_criteria>
  <done>La base de tests existe y cubre el comportamiento principal del conversor actual.</done>
</task>

<task type="auto">
  <name>Task 2: Alinear documentación técnica con la implementación actual</name>
  <files>CLAUDE.md, NOTEBOOK.md</files>
  <read_first>CLAUDE.md, NOTEBOOK.md, extractor_url.py</read_first>
  <action>Actualiza `CLAUDE.md` y `NOTEBOOK.md` para reflejar el estado real del proyecto: pipeline actual con `trafilatura` y `markdownify`, presencia de selector CSS, uso de `threading` en la GUI y prioridad inmediata de la fase de tests.</action>
  <verify>grep -n "threading\|selector\|trafilatura\|markdownify" CLAUDE.md NOTEBOOK.md</verify>
  <acceptance_criteria>
    - `CLAUDE.md` menciona `selector`
    - `CLAUDE.md` menciona `trafilatura`
    - `NOTEBOOK.md` refleja que `threading` en GUI ya está hecho
  </acceptance_criteria>
  <done>La documentación principal no contradice al código actual.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/ -q` succeeds
- [ ] `pylint extractor_url.py` sigue siendo ejecutable sin introducir dependencias no descritas
- [ ] La documentación principal coincide con el estado del código revisado
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- La fase 1 queda lista para evolucionar con base de tests y documentación fiable
</success_criteria>

<output>
After completion, create `.planning/phases/01-validacion-automatica-del-conversor/01-01-SUMMARY.md`
</output>
