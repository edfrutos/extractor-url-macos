---
phase: 01-validacion-automatica-del-conversor
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - tests/conftest.py
  - NOTEBOOK.md
autonomous: true
requirements: [REQ-01, REQ-03]
user_setup: []
gap_closure: true
must_haves:
  truths:
    - "El proyecto puede ejecutar `pytest tests/` desde la raíz del repo sin depender de `python -m`."
    - "NOTEBOOK.md describe correctamente que `requirements.txt` ya está reducido a dependencias reales actuales."
  artifacts:
    - path: "tests/conftest.py"
      provides: "Bootstrap de import path para que pytest cargue `extractor_url` desde la raíz"
    - path: "NOTEBOOK.md"
      provides: "Estado documental alineado con `requirements.txt`"
  key_links:
    - from: "tests/conftest.py"
      to: "extractor_url.py"
      via: "Inserción explícita de la raíz del repo en `sys.path` antes de importar tests"
    - from: "NOTEBOOK.md"
      to: "requirements.txt"
      via: "Referencia literal al estado real de dependencias"
---

<objective>
Cerrar los gaps objetivos detectados en verificación para que la suite declarada se ejecute con `pytest tests/ -q` y la documentación principal deje de contradecir el estado real del repositorio.

Purpose: Recuperar la ejecutabilidad robusta de la fase 1 y dejar el artefacto de estado listo para futuras fases sin ruido documental.
Output: Bootstrap de `pytest` desde la raíz del repo y ajuste puntual de `NOTEBOOK.md`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-validacion-automatica-del-conversor/01-VERIFICATION.md
@.planning/phases/01-validacion-automatica-del-conversor/01-01-SUMMARY.md
@tests/test_converter.py
@NOTEBOOK.md
@requirements.txt

<interfaces>
From tests/test_converter.py:
```python
import extractor_url

def test_clean_soup_elimina_ruido_y_resuelve_urls_relativas() -> None: ...
def test_main_content_prioriza_article_sobre_body() -> None: ...
def test_post_process_markdown_normaliza_unicode_y_ruido() -> None: ...
def test_extract_markdown_con_selector_usa_html_controlado(...) -> None: ...
def test_extract_markdown_sin_selector_hace_fallback_a_main_content(...) -> None: ...
```

From extractor_url.py:
```python
def _clean_soup(soup: BeautifulSoup, base_url: str) -> BeautifulSoup: ...
def _main_content(soup: BeautifulSoup) -> Union[Tag, BeautifulSoup]: ...
def _post_process_markdown(text: str) -> str: ...
def extract_html_structure_to_markdown(
    url: str, selector: Optional[str] = None
) -> Optional[str]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hacer que `pytest` importe `extractor_url` desde la raíz del repo</name>
  <files>tests/conftest.py</files>
  <read_first>tests/test_converter.py, extractor_url.py</read_first>
  <action>Crea `tests/conftest.py` con `from __future__ import annotations`, `import sys` y `from pathlib import Path`. Calcula la raíz del repositorio con `ROOT = Path(__file__).resolve().parents[1]` y, si `str(ROOT)` no está ya en `sys.path`, insértala con `sys.path.insert(0, str(ROOT))` antes de la carga de módulos de test. No cambies `extractor_url.py` ni conviertas la suite a instalación editable: el objetivo del gap es que el entrypoint literal `pytest tests/ -q` funcione desde la raíz sin depender de `python -m`.</action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` contiene `ROOT = Path(__file__).resolve().parents[1]`
    - `tests/conftest.py` contiene `sys.path.insert(0, str(ROOT))`
    - `pytest tests/ -q` termina con exit code 0 desde la raíz del repo
    - La salida ya no contiene `ModuleNotFoundError: No module named 'extractor_url'`
  </acceptance_criteria>
  <done>La suite declarada en la fase se recoge y ejecuta con el binario `pytest` desde la raíz del proyecto.</done>
</task>

<task type="auto">
  <name>Task 2: Corregir el estado de `NOTEBOOK.md` sobre dependencias reales</name>
  <files>NOTEBOOK.md</files>
  <read_first>NOTEBOOK.md, requirements.txt, .planning/phases/01-validacion-automatica-del-conversor/01-VERIFICATION.md</read_first>
  <action>En la sección `## 1. Estado actual — Qué hay`, sustituye la frase que afirma que `requirements.txt` sigue pendiente de limpiarse por una afirmación exacta del estado actual: `requirements.txt` ya está reducido a dependencias reales actuales. Mantén el resto del contexto histórico intacto y no abras nuevas líneas de alcance; el cambio debe eliminar únicamente la contradicción señalada en verificación y dejar consistente el texto con el contenido real de `requirements.txt` (`beautifulsoup4`, `lxml`, `markdownify`, `requests`, `trafilatura`).</action>
  <verify>
    <automated>python -c "from pathlib import Path; text = Path('NOTEBOOK.md').read_text(encoding='utf-8'); assert 'requirements.txt` ya está reducido a dependencias reales actuales' in text; assert 'sigue pendiente dejar `requirements.txt` reducido a dependencias reales.' not in text"</automated>
  </verify>
  <acceptance_criteria>
    - `NOTEBOOK.md` contiene la cadena `requirements.txt` ya está reducido a dependencias reales actuales
    - `NOTEBOOK.md` no contiene la cadena `sigue pendiente dejar `requirements.txt` reducido a dependencias reales.`
    - `requirements.txt` sigue listando solo `beautifulsoup4`, `lxml`, `markdownify`, `requests` y `trafilatura`
  </acceptance_criteria>
  <done>`NOTEBOOK.md` deja de contradecir el contenido real de `requirements.txt` y la documentación principal vuelve a ser fiable para siguientes fases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → import bootstrap | `pytest` ejecuta código auxiliar antes de importar `extractor_url`; cualquier manipulación del path afecta a qué módulo se carga |
| documentation → future planning | `NOTEBOOK.md` influye en futuras decisiones; una afirmación desactualizada propaga decisiones erróneas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | `tests/conftest.py` | mitigate | Insertar únicamente la raíz resuelta del repo derivada de `__file__` (`Path(__file__).resolve().parents[1]`), sin leer rutas de entrada externa ni del cwd |
| T-01-02-02 | I | `NOTEBOOK.md` | mitigate | Basar el texto nuevo en el contenido literal de `requirements.txt` y verificar con una aserción automatizada que elimina la frase obsoleta |
|
</threat_model>

<verification>
- `pytest tests/ -q` pasa desde la raíz del repo
- `python -c "from pathlib import Path; text = Path('NOTEBOOK.md').read_text(encoding='utf-8'); assert 'requirements.txt` ya está reducido a dependencias reales actuales' in text; assert 'sigue pendiente dejar `requirements.txt` reducido a dependencias reales.' not in text"` pasa
</verification>

<success_criteria>
- El gap de ejecutabilidad de `pytest tests/` queda cerrado
- El gap documental de `NOTEBOOK.md` queda cerrado
- No se introducen dependencias nuevas ni cambios funcionales en `extractor_url.py`
</success_criteria>

<output>
After completion, create `.planning/phases/01-validacion-automatica-del-conversor/01-02-SUMMARY.md`
</output>
