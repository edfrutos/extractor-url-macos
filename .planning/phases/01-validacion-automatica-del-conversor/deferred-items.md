# Deferred Items

- 2026-06-03 — El entorno local no expone `pylint` en el `PATH` global y `.venv` no tiene `pytest` instalado. No bloquea el cambio entregado, pero conviene alinear la toolchain del proyecto en una fase de mantenimiento.
