---
phase: 06-export-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core.py
  - extractor_url.py
  - tests/test_cli.py
autonomous: true
requirements: [EXPORT-04]
must_haves:
  truths:
    - "El modo --json del CLI incluye siempre el campo title (string o null)"
    - "Una página con <title> produce title con el texto del título; sin <title> produce null"
    - "El JSON existente (status/url/content/char_count/output_type) no se rompe"
  artifacts:
    - path: "core.py"
      provides: "Función _extract_title(soup, html_text) que devuelve Optional[str]"
      contains: "def _extract_title"
    - path: "extractor_url.py"
      provides: "Bloque JSON de éxito ampliado con campo title"
      contains: "\"title\""
    - path: "tests/test_cli.py"
      provides: "Tests del contrato JSON con title presente y null"
      contains: "title"
  key_links:
    - from: "extractor_url.py"
      to: "core._extract_title"
      via: "import + llamada antes del bloque JSON de éxito"
      pattern: "_extract_title"
---

<objective>
Ampliar el contrato JSON del CLI Python (`--json`) con el campo `title` (el `<title>`
de la página), implementando D-05 y D-07 (fallback). Este campo es la fuente de verdad
que el lado Swift consumirá para el nombre de archivo sugerido en los tres formatos de
exportación.

Purpose: Sin `title` en el JSON, el NSSavePanel de PDF/MD/HTML no puede sugerir un
nombre derivado del título de la página (Success Criteria 3 de la fase). El campo es
opcional y retrocompatible: un decoder Swift `String?` tolera `null`.

Output: `core._extract_title()`, bloque JSON de éxito con `title`, tests del contrato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-export-pdf/06-CONTEXT.md
@.planning/phases/06-export-pdf/06-RESEARCH.md
@.planning/phases/06-export-pdf/06-PATTERNS.md

<interfaces>
<!-- Firmas existentes verificadas en código (2026-06-12). Usar directamente. -->

De core.py:
- def _fetch_raw(url: str, timeout: int = 15, use_cache: bool = True) -> Optional[tuple[str, str]]
- def _fetch_soup(url: str, timeout: int = 15, use_cache: bool = True) -> Optional[BeautifulSoup]
- def extract_formatted_content(url, return_type, selector, timeout, use_cache) -> ...
- Imports ya presentes: trafilatura, from bs4 import BeautifulSoup, FeatureNotFound

De extractor_url.py:
- Línea 20: from core import extract_formatted_content   (AMPLIAR este import)
- Línea 18: from bs4 import BeautifulSoup                 (ya importado)
- Líneas 290-298: bloque `if args.json:` de éxito con _print_json_output (AMPLIAR)
- args.use_cache existe (set_defaults use_cache=True, línea 254)
- args.timeout existe

De tests/test_cli.py:
- Línea 34: test_main_json_devuelve_salida_estructurada (patrón monkeypatch + pytest.raises(SystemExit, match="0") + json.loads(capsys...))
- Imports: builtins, json, sys, pytest, import extractor_url
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _extract_title en core.py</name>
  <files>core.py</files>
  <read_first>
    - core.py (estado actual: imports en líneas 8-25; helpers privados _fetch_raw línea 76, _apply_selector ~171, _post_process_markdown ~187, _format_soup_content ~197)
    - .planning/phases/06-export-pdf/06-RESEARCH.md (Pattern 4 + Pitfall 5: guard doble `soup.title and soup.title.string`)
    - .planning/phases/06-export-pdf/06-PATTERNS.md (sección core.py: estructura helper privado + except broad en frontera)
  </read_first>
  <behavior>
    - _extract_title con soup que tiene `<title>Mi Título</title>` devuelve "Mi Título"
    - _extract_title con soup `<title></title>` (vacío) y html_text con og:title devuelve el título de trafilatura
    - _extract_title con soup `<title>  Espacios  </title>` devuelve "Espacios" (strip aplicado)
    - _extract_title sin <title> y sin html_text (o trafilatura sin metadata) devuelve None
  </behavior>
  <action>
    Crear la función `_extract_title(soup: BeautifulSoup, html_text: Optional[str] = None) -> Optional[str]`
    en core.py, ubicada entre `_post_process_markdown` y `_format_soup_content` dentro del bloque
    de utilidades. Lógica en dos caminos: Camino 1 (BeautifulSoup) usa el guard doble
    `soup.title and soup.title.string`, aplica `.strip()` y devuelve el resultado si no queda vacío
    (Pitfall #5 de RESEARCH.md). Camino 2 (fallback) si Camino 1 no produjo título y `html_text` no es
    None: llamar `trafilatura.extract_metadata(html_text)` dentro de un `try/except Exception` con
    `# pylint: disable=broad-exception-caught` (patrón frontera de _fetch_raw); si `meta and meta.title`,
    devolver `meta.title.strip()`. Si ningún camino produce título, devolver `None`. Tipado completo
    `Optional[str]`, docstring en español, línea máx. 88 chars. Los imports `trafilatura` y `BeautifulSoup`
    ya están presentes — no añadir imports.
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && source .venv/bin/activate && python -c "from core import _extract_title; from bs4 import BeautifulSoup; assert _extract_title(BeautifulSoup('<title>X</title>','html.parser'))=='X'; assert _extract_title(BeautifulSoup('<body></body>','html.parser'))is None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - core.py contiene `def _extract_title(`
    - `_extract_title(BeautifulSoup('<title>X</title>','html.parser'))` devuelve `"X"`
    - `_extract_title(BeautifulSoup('<title>  X  </title>','html.parser'))` devuelve `"X"` (strip)
    - `_extract_title(BeautifulSoup('<body></body>','html.parser'))` devuelve `None`
    - El bloque trafilatura usa `except Exception:  # pylint: disable=broad-exception-caught`
    - `pylint core.py` no añade errores nuevos respecto al baseline
  </acceptance_criteria>
  <done>_extract_title existe, cubre los 4 comportamientos del bloque behavior y no rompe pylint.</done>
</task>

<task type="auto">
  <name>Task 2: Ampliar bloque JSON de éxito con title en extractor_url.py</name>
  <files>extractor_url.py</files>
  <read_first>
    - extractor_url.py (línea 20 import de core; líneas 290-298 bloque JSON de éxito; línea 127-136 patrón _fetch_soup con FeatureNotFound; args.use_cache y args.timeout disponibles)
    - core.py (firma de _fetch_raw y _extract_title del Task 1)
    - .planning/phases/06-export-pdf/06-PATTERNS.md (sección extractor_url.py: bloque de extracción de title con parser fallback lxml→html.parser)
  </read_first>
  <action>
    Ampliar el import de la línea 20 a `from core import extract_formatted_content, _fetch_raw, _extract_title`.
    Asegurar que `FeatureNotFound` está importado desde bs4 (la línea 18 ya importa `BeautifulSoup`; añadir
    `FeatureNotFound` al mismo import si no está). En el bloque `if args.json:` de éxito (líneas 290-298),
    ANTES de llamar a `_print_json_output`, extraer el título de forma independiente: declarar
    `page_title: Optional[str] = None`, llamar `raw_result = _fetch_raw(args.url, timeout=args.timeout, use_cache=args.use_cache)`
    (segunda llamada = hit de caché cuando use_cache=True; A2 de RESEARCH.md), y si `raw_result` no es None,
    desempaquetar `html_text, _ = raw_result`, construir el soup con fallback `BeautifulSoup(html_text, "lxml")`
    dentro de try / `except FeatureNotFound:` → `BeautifulSoup(html_text, "html.parser")`, y asignar
    `page_title = _extract_title(title_soup, html_text)`. Añadir `"title": page_title` como última clave del
    dict pasado a `_print_json_output`. NO tocar el bloque JSON de error (líneas 280-285) — title solo en éxito.
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && source .venv/bin/activate && python -c "import ast,sys; ast.parse(open('extractor_url.py').read()); src=open('extractor_url.py').read(); assert '_extract_title' in src and '\"title\"' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - extractor_url.py importa `_extract_title` y `_fetch_raw` desde core
    - El dict de `_print_json_output` del path de éxito contiene la clave `"title"`
    - El bloque de error JSON (status error) NO contiene `"title"`
    - `python -c "import extractor_url"` no lanza ImportError
    - El parser usa fallback `except FeatureNotFound` → `html.parser`
  </acceptance_criteria>
  <done>El JSON de éxito emite title; imports correctos; el bloque de error intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests del contrato JSON con title (presente y null)</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py (test_main_json_devuelve_salida_estructurada línea 34: patrón monkeypatch.setattr + pytest.raises(SystemExit, match="0") + json.loads(capsys.readouterr().out))
    - extractor_url.py (cómo se llama _fetch_raw y _extract_title en el path JSON, del Task 2)
    - .planning/phases/06-export-pdf/06-PATTERNS.md (sección tests/test_cli.py: dos tests nuevos con monkeypatch de core._fetch_raw y core._extract_title)
  </read_first>
  <behavior>
    - test_main_json_devuelve_salida_estructurada: añade `assert "title" in output` (campo siempre presente)
    - test nuevo título presente: monkeypatch core._extract_title → "Mi Título" produce output["title"] == "Mi Título"
    - test nuevo título null: monkeypatch core._extract_title → None produce output["title"] is None
  </behavior>
  <action>
    En el test existente `test_main_json_devuelve_salida_estructurada` (línea 34), añadir tras las
    assertions actuales: `assert "title" in output`. Añadir dos tests nuevos siguiendo el mismo esqueleto
    (monkeypatch sys.argv con `--json --no-cache`, monkeypatch `extractor_url.extract_formatted_content`
    devolviendo "contenido", `pytest.raises(SystemExit, match="0")`, json.loads de capsys). En ambos hacer
    `import core` y `monkeypatch.setattr(core, "_fetch_raw", lambda *_a, **_kw: ("<html><title>Mi Título</title></html>", "https://example.com"))`
    y `monkeypatch.setattr(core, "_extract_title", ...)`. Test 1 `test_main_json_incluye_title_cuando_hay_titulo`:
    _extract_title → "Mi Título", asserta `output["title"] == "Mi Título"`. Test 2
    `test_main_json_title_null_sin_titulo`: _extract_title → None, asserta `output["title"] is None`.
    Mantener tipado y estilo del fichero (NoReturn no aplica aquí; usar las firmas pytest.MonkeyPatch / CaptureFixture).
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && source .venv/bin/activate && pytest tests/test_cli.py -k json -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cli.py contiene `test_main_json_incluye_title_cuando_hay_titulo`
    - tests/test_cli.py contiene `test_main_json_title_null_sin_titulo`
    - `pytest tests/test_cli.py -k json -x` sale con código 0
    - `pytest tests/ -x -q` (suite Python completa) sale con código 0
    - El test estructurado original incluye `assert "title" in output`
  </acceptance_criteria>
  <done>Los tres tests (uno ampliado, dos nuevos) pasan; `pytest tests/` verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL remota → core.py | HTML no confiable descargado de la web entra al parser |
| core.py → JSON stdout | El title extraído del HTML sale por el contrato JSON hacia Swift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `_extract_title` parseando HTML no confiable | accept | BeautifulSoup/trafilatura no ejecutan el HTML; el title es string display-only, no se interpola en código ni SQL |
| T-06-02 | Information Disclosure | title en JSON | accept | El title es contenido público de la página; sin PII nueva, sin secretos |
| T-06-03 | Denial of Service | `trafilatura.extract_metadata` sobre HTML malformado | mitigate | try/except Exception envuelve la llamada; fallo → None, nunca excepción no controlada |
| T-06-SC | Tampering | instalaciones npm/pip/cargo | accept | No se instalan paquetes nuevos (RESEARCH.md: sección Package Legitimacy Audit no aplica) |
</threat_model>

<verification>
- `pytest tests/ -x -q` verde (suite Python completa)
- `python -c "import extractor_url, core"` sin errores
- El JSON de éxito incluye `title`; el de error no
</verification>

<success_criteria>
- D-05 implementado: `--json` emite siempre `title` (string o null)
- D-07 (fallback) cubierto en _extract_title: BeautifulSoup → trafilatura → None
- Contrato JSON retrocompatible; tests Python verdes
</success_criteria>

<output>
Create `.planning/phases/06-export-pdf/06-01-SUMMARY.md` when done
</output>
