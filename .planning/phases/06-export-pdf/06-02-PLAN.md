---
phase: 06-export-pdf
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ExtractorApp/ExtractorApp/ExtractorApp/Models/ExtractionResult.swift
  - ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift
  - ExtractorApp/ExtractorApp/ExtractorAppTests/ViewModelTests.swift
autonomous: true
requirements: [EXPORT-04]
must_haves:
  truths:
    - "ExtractionResult decodifica el campo title del JSON (string o null) sin romper el JSON sin title"
    - "El ViewModel conserva el title del último resultado en pageTitle"
    - "suggestedFilename prefiere el title saneado; sin title usa prefijo de contenido; sin nada usa export"
    - "exportMarkdown y exportHTML usan el nuevo suggestedFilename(title:extension:)"
  artifacts:
    - path: "ExtractorApp/ExtractorApp/ExtractorApp/Models/ExtractionResult.swift"
      provides: "Campo opcional title en la struct Codable"
      contains: "let title: String?"
    - path: "ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift"
      provides: "pageTitle @Published + suggestedFilename(title:extension:) unificado"
      contains: "suggestedFilename(title:"
    - path: "ExtractorApp/ExtractorApp/ExtractorAppTests/ViewModelTests.swift"
      provides: "Tests de suggestedFilename (title / fallback contenido / fallback export)"
      contains: "SuggestedFilename"
  key_links:
    - from: "ExtractionViewModel.extract()"
      to: "ExtractionResult.title"
      via: "asignación a pageTitle al recibir el resultado"
      pattern: "pageTitle"
    - from: "exportMarkdown/exportHTML"
      to: "suggestedFilename(title:extension:)"
      via: "llamada con pageTitle"
      pattern: "suggestedFilename\\(title:"
---

<objective>
Establecer el contrato del nombre de archivo y el campo title en la capa Swift, sin tocar
todavía la generación PDF. Implementa D-05 (lado Swift), D-06 (suggestedFilename unificado) y
D-07 (cadena de fallback). Es la base que el Plan 03 (export PDF + UI) consume.

Purpose: Centralizar la lógica de nombre sugerido en un único método que prefiera el título
de la página, para que los tres formatos (MD, HTML, PDF) compartan el mismo comportamiento
(Success Criteria 3 de la fase). El campo title llega del JSON ampliado en el Plan 01.

Output: `ExtractionResult.title`, `ExtractionViewModel.pageTitle`, `suggestedFilename(title:extension:)`,
tests unitarios del nombre sugerido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-export-pdf/06-CONTEXT.md
@.planning/phases/06-export-pdf/06-RESEARCH.md
@.planning/phases/06-export-pdf/06-PATTERNS.md
@.planning/phases/06-export-pdf/06-UI-SPEC.md

<interfaces>
<!-- Estado actual verificado en código (2026-06-12). -->

ExtractionResult.swift (struct Codable actual, sin title):
- Campos: status, url, selector(String?), outputType(String?), charCount(Int?), content(String?), errorMessage(String?)
- CodingKeys: status, url, selector, content / output_type / char_count / error_message
- var isSuccess: Bool { status == "success" }

ExtractionViewModel.swift (verificado):
- @MainActor final class : ObservableObject
- @Published resultContent: String?  (linea 18)
- @Published exportFormat: String = "markdown"  (linea 22)
- extract() linea 36: en exito hace `self?.resultContent = result?.content` (MainActor.run, linea 62)
- exportMarkdown() linea 155: usa suggestedFilename(from: content, extension: "md") linea 161
- exportHTML() linea 178: usa suggestedFilename(from: content, extension: "html") linea 184
- suggestedFilename(from:extension:) linea 200 — PRIVATE, heuristica prefijo 50 chars then "export"
- export() switch linea 140: case "pdf": break (NO tocar en este plan — lo activa Plan 03)

ViewModelTests.swift:
- @MainActor final class ViewModelTests
- testExportDispatchMarkdown linea 59 (patron async test + rama no-op)
- Usa @testable import del modulo de la app
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Campo title en ExtractionResult</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp/Models/ExtractionResult.swift</files>
  <read_first>
    - ExtractorApp/ExtractorApp/ExtractorApp/Models/ExtractionResult.swift (struct completa, lineas 1-20)
    - .planning/phases/06-export-pdf/06-PATTERNS.md (seccion ExtractionResult.swift: anadir title a campos Y a CodingKeys, sin alias)
  </read_first>
  <behavior>
    - Decodificar JSON con "title":"X" produce result.title == "X"
    - Decodificar JSON con "title":null produce result.title == nil
    - Decodificar JSON SIN campo title (retrocompat) produce result.title == nil sin lanzar
  </behavior>
  <action>
    Anadir `let title: String?` a la struct ExtractionResult, justo despues de `errorMessage`. Anadir
    `title` al case de CodingKeys que NO lleva alias (la linea `case status, url, selector, content` pasa a
    `case status, url, selector, content, title`) — "title" en JSON coincide con "title" en Swift, sin
    rawValue. No tocar los CodingKeys con alias (output_type, char_count, error_message). El campo opcional
    garantiza retrocompatibilidad: JSON sin title decodifica a nil sin error.
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && grep -q "let title: String?" ExtractorApp/ExtractorApp/ExtractorApp/Models/ExtractionResult.swift && grep -E "case status, url, selector, content, title" ExtractorApp/ExtractorApp/ExtractorApp/Models/ExtractionResult.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ExtractionResult.swift contiene `let title: String?`
    - El CodingKeys sin alias incluye `title` (linea `case status, url, selector, content, title`)
    - Los CodingKeys con alias (output_type, char_count, error_message) permanecen sin cambios
  </acceptance_criteria>
  <done>El campo title opcional existe en la struct y en CodingKeys; retrocompatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pageTitle + suggestedFilename unificado en el ViewModel</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift</files>
  <read_first>
    - ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift (lineas 7-72 estado y extract; 155-207 exportMarkdown/exportHTML/suggestedFilename)
    - .planning/phases/06-export-pdf/06-PATTERNS.md (seccion ExtractionViewModel.swift: refactor suggestedFilename, pageTitle, actualizacion de llamadas)
    - .planning/phases/06-export-pdf/06-UI-SPEC.md (algoritmo de nombre sugerido D-06/D-07: sanitizacion [^a-zA-Z0-9_\-\s] then "-", espacios then "-", prefix(60), trim "-")
  </read_first>
  <behavior>
    - suggestedFilename(title:"Mi Articulo!",extension:"pdf") produce nombre saneado con espacios convertidos a "-" y extension .pdf
    - suggestedFilename(title:nil,...) con resultContent="Texto de prueba" usa prefijo de contenido, NO "export"
    - suggestedFilename(title:nil,...) con resultContent=nil produce "export.<ext>"
    - suggestedFilename(title:"",...) (vacio) cae al fallback de contenido
  </behavior>
  <action>
    Anadir `@Published var pageTitle: String? = nil` junto a los @Published existentes (tras linea 22).
    En extract(), dentro del bloque de exito de MainActor.run (linea 62, donde se asigna resultContent),
    anadir `self?.pageTitle = result?.title`. Reemplazar el metodo privado
    `suggestedFilename(from:extension:)` (lineas 200-207) por `suggestedFilename(title: String?, extension ext: String) -> String`
    con visibilidad `internal` (NO private — los tests con @testable import lo necesitan; ver
    PATTERNS.md nota de acoplamiento). Logica D-06/D-07: (1) si title no es nil ni vacio, sanear con
    regex `[^a-zA-Z0-9_\-\s]` then "-", trim whitespacesAndNewlines, prefix(60), espacios then "-"; si el
    resultado no queda vacio, devolver el nombre saneado con la extension. (2) Fallback: si resultContent
    existe, prefijo de 50 chars saneado con `[^a-zA-Z0-9_\-]` then "-" y trim "-"; si no vacio, devolver.
    (3) `"export.<ext>"`. Actualizar las dos llamadas existentes: exportMarkdown (linea 161) y exportHTML
    (linea 184) pasan de `suggestedFilename(from: content, extension: ext)` a
    `suggestedFilename(title: pageTitle, extension: ext)`. NO tocar el switch export() (case "pdf": break
    sigue igual — lo activa el Plan 03).
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && grep -q "pageTitle" ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift && grep -q "func suggestedFilename(title:" ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift && ! grep -q "suggestedFilename(from:" ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ExtractionViewModel.swift contiene `@Published var pageTitle: String?`
    - extract() asigna `self?.pageTitle = result?.title` en el path de exito
    - Existe `func suggestedFilename(title: String?, extension ext: String) -> String` (internal, no private)
    - NO queda ninguna referencia a `suggestedFilename(from:` en el fichero
    - exportMarkdown y exportHTML llaman `suggestedFilename(title: pageTitle, extension:`
    - El switch export() conserva `case "pdf":` con `break` (sin cambios en este plan)
  </acceptance_criteria>
  <done>pageTitle poblado en extract(); suggestedFilename unificado e internal; MD/HTML migrados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de suggestedFilename</name>
  <files>ExtractorApp/ExtractorApp/ExtractorAppTests/ViewModelTests.swift</files>
  <read_first>
    - ExtractorApp/ExtractorApp/ExtractorAppTests/ViewModelTests.swift (testExportDispatchMarkdown linea 59: patron @MainActor async test, @testable import)
    - .planning/phases/06-export-pdf/06-PATTERNS.md (seccion ViewModelTests.swift: tests 2-4 de suggestedFilename; nota de que suggestedFilename debe ser internal)
    - .planning/phases/06-export-pdf/06-VALIDATION.md (Wave 0: testSuggestedFilenameWithTitle, testSuggestedFilenameFallbackContent, testSuggestedFilenameFallbackExport)
  </read_first>
  <behavior>
    - testSuggestedFilenameWithTitle: pageTitle="Mi Articulo de Prueba" -> nombre contiene el titulo saneado (no "export")
    - testSuggestedFilenameFallbackToContent: pageTitle=nil, resultContent="Contenido sin titulo" -> nombre derivado del contenido (no "export")
    - testSuggestedFilenameFallbackToExport: pageTitle=nil, resultContent=nil -> "export.<ext>"
  </behavior>
  <action>
    Anadir tres tests `@MainActor` a la clase ViewModelTests, llamando directamente al metodo
    `suggestedFilename(title:extension:)` (accesible porque el Task 2 lo hace internal y la clase de test
    usa @testable import). testSuggestedFilenameWithTitle: instanciar ExtractionViewModel, set pageTitle a
    "Mi Articulo de Prueba", llamar `vm.suggestedFilename(title: vm.pageTitle, extension: "pdf")`, assert
    que el resultado termina en ".pdf", NO es "export.pdf", y no contiene espacios. testSuggestedFilenameFallbackToContent:
    pageTitle=nil, resultContent="Contenido sin titulo", assert que el resultado NO es "export.md" y termina
    en ".md". testSuggestedFilenameFallbackToExport: pageTitle=nil, resultContent=nil, assert resultado ==
    "export.txt". Usar XCTAssertEqual / XCTAssertTrue / XCTAssertNotEqual. Seguir el estilo del fichero
    (sin red, sin NSSavePanel — solo el metodo puro).
  </action>
  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && xcodebuild test -scheme ExtractorApp -only-testing:ExtractorAppTests/ViewModelTests -quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - ViewModelTests.swift contiene testSuggestedFilenameWithTitle, testSuggestedFilenameFallbackToContent, testSuggestedFilenameFallbackToExport
    - testSuggestedFilenameFallbackToExport asserta resultado == "export.txt"
    - `xcodebuild test -scheme ExtractorApp -only-testing:ExtractorAppTests/ViewModelTests` termina con TEST SUCCEEDED
  </acceptance_criteria>
  <done>Los tres tests de suggestedFilename pasan en XCTest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON (Python) to ExtractionResult | El title viene del HTML remoto y se decodifica en Swift |
| title to nombre de archivo | El title se usa como nombre de fichero en disco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | title usado como filename (path traversal via "../") | mitigate | suggestedFilename sanea con regex `[^a-zA-Z0-9_\-\s]` then "-": "/" y "." de traversal se eliminan antes de usarse |
| T-06-05 | Information Disclosure | title decodificado de JSON no confiable | accept | title es string display/filename; sin ejecucion, sin interpolacion en codigo |
| T-06-SC | Tampering | instalaciones npm/pip/cargo | accept | No se instalan paquetes nuevos (RESEARCH.md Package Legitimacy Audit no aplica) |
</threat_model>

<verification>
- `xcodebuild test -scheme ExtractorApp -only-testing:ExtractorAppTests/ViewModelTests` TEST SUCCEEDED
- ExtractionResult decodifica title; pageTitle poblado; suggestedFilename unificado
- No quedan llamadas a la firma antigua suggestedFilename(from:)
</verification>

<success_criteria>
- D-05 (Swift), D-06 y D-07 implementados
- Los tres formatos comparten suggestedFilename(title:extension:)
- Tests de nombre sugerido verdes en XCTest
</success_criteria>

<output>
Create `.planning/phases/06-export-pdf/06-02-SUMMARY.md` when done
</output>
