---
phase: 15-flag-manual-js
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core.py
  - extractor_url.py
  - tests/test_flag_js.py
  - tests/test_cli.py
autonomous: true
requirements:
  - FLAG-01
  - FLAG-02

must_haves:
  truths:
    - "Sin pasar --js ni --no-js, el comportamiento es exactamente el de v4.0 (js_mode='auto', heurística _looks_insufficient() sin cambios)"
    - "--js fuerza _fetch_via_playwright() aunque _looks_insufficient() devuelva False"
    - "--no-js nunca llama a _fetch_via_playwright(), aunque _looks_insufficient() devuelva True"
    - "--js/--no-js tienen efecto real incluso si la URL ya está cacheada (saltan la LECTURA de caché, no la escritura)"
    - "--js y --no-js a la vez fallan explícito vía argparse (SystemExit código 2), sin código de validación manual"
    - "js_mode se propaga a _lookup_title() igual que use_cache, para no repetir descarga/render en la segunda llamada"
  artifacts:
    - path: "core.py"
      provides: "js_mode: str = 'auto' en _fetch_raw/_fetch_soup/extract_formatted_content/extract_html_structure_to_markdown"
      contains: "js_mode"
    - path: "extractor_url.py"
      provides: "--js/--no-js mutuamente excluyentes, _resolve_js_mode(), propagación en main()/_run_batch()/_lookup_title()"
      contains: "add_mutually_exclusive_group"
    - path: "tests/test_flag_js.py"
      provides: "tests de js_mode en _fetch_raw (force/off/auto, bypass de caché)"
      contains: "def test_fetch_raw_js_mode_force"
  key_links:
    - from: "extractor_url.py main()/_run_batch()"
      to: "core._fetch_raw (vía extract_formatted_content/_lookup_title)"
      via: "js_mode=_resolve_js_mode(args)"
      pattern: "_resolve_js_mode"
---

<objective>
Añadir `--js`/`--no-js` a la CLI para forzar u omitir explícitamente el
fallback Playwright (Fase 11, v4.0), sin cambiar el comportamiento por
defecto (heurística automática `_looks_insufficient()` intacta si no se
pasa ninguno de los dos flags). Cierra FLAG-01/FLAG-02 y la Fase 15.

Purpose: Dar al usuario control explícito sobre cuándo pagar el coste de
Playwright (forzarlo en sitios que la heurística no detecta bien) o
evitarlo siempre (sitios donde nunca hace falta y se quiere ahorrar
latencia), sin depender únicamente de un umbral de caracteres.
Output: `core.py`/`extractor_url.py` extendidos, tests nuevos, suite
completa en verde (pytest + pylint + mypy), sin checkpoint humano (fase
solo-CLI, sin app SwiftUI).
</objective>

<execution_context>
@/Users/edefrutos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-flag-manual-js/15-RESEARCH.md
</context>

<interfaces>
`_fetch_raw()` (core.py:116-171) — punto de integración único, ya con
`use_cache: bool = True` como precedente exacto del patrón de propagación
a seguir para `js_mode: str = "auto"`.

`_looks_insufficient()`/`_fetch_via_playwright()` (core.py:186-231) —
sin cambios, se reutilizan tal cual desde los 3 caminos de `js_mode`.

Cadena de propagación de `use_cache` (a replicar para `js_mode`):
```
extractor_url.main() [args.use_cache]
  → extract_formatted_content(..., use_cache=args.use_cache)
      → extract_html_structure_to_markdown(..., use_cache=use_cache)
      → _fetch_soup(..., use_cache=use_cache)
          → _fetch_raw(..., use_cache=use_cache)
  → extract_html_structure_to_markdown(..., use_cache=use_cache)  [directo]
      → _fetch_raw(..., use_cache=use_cache)
```

`_lookup_title()` (extractor_url.py:29-34) y `_run_batch()` — mismos 2
call-sites de `extract_formatted_content`/`_lookup_title` que ya reciben
`use_cache=args.use_cache`, necesitan `js_mode=` añadido en paralelo.

`--no-cache` (extractor_url.py:295-300) — precedente de flag booleano
simple (`action="store_false", dest="use_cache"`); `--js`/`--no-js` usan
en cambio `add_mutually_exclusive_group()` (nuevo en este archivo, API
estándar de `argparse`, sin código de validación manual).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core.py — js_mode en _fetch_raw() y cadena de propagación</name>
  <files>core.py, tests/test_flag_js.py</files>

  <read_first>
    core.py — `_fetch_raw()` (116-171), `_fetch_soup()` (174-183), `extract_formatted_content()`/`extract_html_structure_to_markdown()` (354-390)
    tests/test_js_fallback.py — patrón de mocking a replicar (monkeypatch de `_HTTP_SESSION.get` y `_fetch_via_playwright`)
    .planning/phases/15-flag-manual-js/15-RESEARCH.md — sección "Architecture Patterns", código citado casi literal
  </read_first>

  <behavior>
    - `_fetch_raw(url, js_mode="force", use_cache=False)` con HTML rico simulado (`_HTTP_SESSION.get` mockeado) y `_fetch_via_playwright` mockeado devolviendo un HTML distinto reconocible → devuelve el HTML del mock de Playwright, no el rico original (fuerza pese a heurística negativa).
    - `_fetch_raw(url, js_mode="off", use_cache=False)` con HTML pobre simulado (SPA vacía) y `_fetch_via_playwright` mockeado para lanzar `AssertionError` si se llama → devuelve el HTML pobre tal cual, Playwright nunca invocado.
    - `_fetch_raw(url, js_mode="auto", ...)` con HTML rico/pobre → comportamiento idéntico a los tests ya existentes de `tests/test_js_fallback.py` (no-regresión; esos tests no se tocan y deben seguir pasando).
    - `_fetch_raw(url, js_mode="force")` con Playwright no disponible (`_fetch_via_playwright` mockeado devolviendo `None`) → degrada al HTML estático sin lanzar excepción (mismo principio JS-03).
    - Bypass de caché: primera llamada con `js_mode="auto"`, `use_cache=True` sobre una URL cachea HTML estático (usar `tmp_path`/monkeypatch de `_CACHE_DIR` para no tocar la caché real); segunda llamada a la MISMA URL con `js_mode="force"` debe invocar `_fetch_via_playwright` pese al cache hit disponible (monkeypatchear `_fetch_via_playwright` para lanzar si no se llama, o para devolver un HTML reconocible y verificar que se devuelve ese, no el cacheado).
  </behavior>

  <action>
  En `core.py`:
  1. `_fetch_raw()` gana el parámetro `js_mode: str = "auto"`. La lectura de
     caché (`if use_cache: ... if cache_file.exists(): return ...`) se
     condiciona a `js_mode == "auto"` además del `use_cache` ya existente
     (saltar solo la LECTURA, la escritura al final no cambia).
  2. Sustituir la línea `if _looks_insufficient(html_text):` por la
     decisión de 3 vías descrita en `15-RESEARCH.md` (`force` → siempre
     Playwright; `off` → nunca; `auto` → heurística sin cambios).
  3. `_fetch_soup()`, `extract_formatted_content()` y
     `extract_html_structure_to_markdown()` añaden `js_mode: str = "auto"`
     y lo reenvían, exactamente en paralelo a como ya hacen con
     `use_cache`.
  4. Actualizar el docstring de `_fetch_raw()` para documentar `js_mode`.
  </action>

  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && pytest tests/test_flag_js.py tests/test_js_fallback.py -v</automated>
  </verify>

  <acceptance_criteria>
    - `js_mode="force"` invoca Playwright pese a HTML rico (heurística negativa).
    - `js_mode="off"` nunca invoca Playwright pese a HTML pobre (heurística positiva).
    - `js_mode="auto"` sin cambios frente al comportamiento ya existente — los 4 tests de `tests/test_js_fallback.py` siguen pasando sin modificarlos.
    - `js_mode != "auto"` salta la lectura de caché mientras `js_mode="auto"` la preserva intacta.
  </acceptance_criteria>

  <done>`_fetch_raw()` y toda la cadena de propagación soportan `js_mode`, con tests que cubren las 3 vías y el bypass de caché.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extractor_url.py — flags --js/--no-js + propagación CLI</name>
  <files>extractor_url.py, tests/test_cli.py</files>

  <read_first>
    extractor_url.py — bloque `argparse` completo (265-317), `main()` (319-390), `_run_batch()`, `_lookup_title()` (29-34)
    tests/test_cli.py — patrón de monkeypatch de `sys.argv` + `extractor_url.extract_formatted_content`
    .planning/phases/15-flag-manual-js/15-RESEARCH.md — sección "Architecture Patterns", `_resolve_js_mode()`
  </read_first>

  <behavior>
    - `sys.argv = [..., "--js"]` → `extract_formatted_content` recibe `js_mode="force"` (inspeccionar kwargs de la llamada mockeada).
    - `sys.argv = [..., "--no-js"]` → recibe `js_mode="off"`.
    - `sys.argv = [...]` sin ninguno de los dos → recibe `js_mode="auto"`.
    - `sys.argv = [..., "--js", "--no-js"]` → `SystemExit` código 2 (mecanismo nativo de `argparse.add_mutually_exclusive_group()`, sin `pytest.raises` sobre un mensaje concreto, solo el código de salida).
    - `_lookup_title()` recibe el mismo `js_mode` que `extract_formatted_content` en la misma invocación (evitar la repetición de descarga/render del Pitfall 2 del research).
    - `--batch` con `--js`/`--no-js` aplica el mismo `js_mode` a todas las URLs del lote (una sola resolución de flags, no per-línea).
  </behavior>

  <action>
  En `extractor_url.py`:
  1. Añadir al parser un grupo mutuamente excluyente:
     `js_group = parser.add_mutually_exclusive_group()` con `--js`
     (`action="store_true"`) y `--no-js` (`action="store_true"`, `dest="no_js"`).
  2. Añadir `_resolve_js_mode(args: argparse.Namespace) -> str` que
     devuelve `"force"`/`"off"`/`"auto"` según `args.js`/`args.no_js`.
  3. En `main()`: calcular `js_mode = _resolve_js_mode(args)` una vez,
     pasarlo a la llamada de `extract_formatted_content(...)` y a
     `_lookup_title(args.url, args.timeout, args.use_cache, js_mode=...)`
     (añadir el parámetro `js_mode` a la firma de `_lookup_title()`,
     reenviado a su `_fetch_raw()` interno).
  4. En `_run_batch()`: mismo `js_mode` calculado una vez, pasado en las
     llamadas de `extract_formatted_content(...)` y `_lookup_title(...)`
     dentro del bucle sobre las URLs del archivo.
  5. Ayuda de `--help` para ambos flags, en español, describiendo el
     efecto exacto (ver research, sección "Architecture Patterns").
  </action>

  <verify>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url && pytest tests/ -v && pylint core.py extractor_url.py && mypy core.py extractor_url.py</automated>
  </verify>

  <acceptance_criteria>
    - `--js`/`--no-js` mutuamente excluyentes vía `argparse`, sin validación manual.
    - `js_mode` llega correctamente a `extract_formatted_content` y `_lookup_title` en `main()` y `_run_batch()`.
    - Suite completa (`pytest tests/`) en verde, `pylint`/`mypy` sin warnings nuevos.
    - Sin flags, comportamiento idéntico a v4.0 (no-regresión confirmada por los tests ya existentes).
  </acceptance_criteria>

  <done>CLI expone `--js`/`--no-js`, ambos funcionando end-to-end desde `main()` y `_run_batch()`, suite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flags CLI (`--js`/`--no-js`) → `js_mode` interno | Entrada de un usuario local con acceso ya total a la CLI — mismo nivel de confianza que `--selector`/`--timeout` ya existentes, sin superficie nueva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Combinación inválida `--js --no-js` | mitigate | `add_mutually_exclusive_group()` de `argparse` falla nativo con `SystemExit(2)` — sin lógica de validación propia que pueda tener bugs. |
| T-15-02 | Denial of Service (local) | `--js` fuerza Playwright en un bucle `--batch` grande | accept | Mismo coste ya aceptado en v4.0 para URLs que activan la heurística automáticamente — `--js` solo lo hace determinista, no añade una superficie de recursos nueva. Timeout explícito (`timeout * 1000` ms) ya limita el peor caso por URL. |
</threat_model>

<verification>
```bash
cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url
pytest tests/ -v
pylint core.py extractor_url.py
mypy core.py extractor_url.py
```
</verification>

<success_criteria>
1. Sin flags, comportamiento idéntico a v4.0 (heurística automática, sin cambios) — confirmado por los tests ya existentes de `tests/test_js_fallback.py` sin modificar.
2. `--js` fuerza el render con Playwright aunque la heurística automática no lo hubiera activado.
3. `--no-js` desactiva el fallback Playwright aunque la heurística automática sí lo activaría.
4. `--js`/`--no-js` tienen efecto real incluso sobre una URL ya cacheada.
5. `pytest tests/` + `pylint` + `mypy` en verde, sin regresiones.
</success_criteria>

<output>
Crear `.planning/phases/15-flag-manual-js/15-01-SUMMARY.md` al completar ambas tareas con la suite en verde.
</output>
