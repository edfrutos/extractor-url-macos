---
phase: "03"
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift
  - ExtractorApp/ExtractorApp/ExtractorApp/Views/SettingsView.swift
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
  - SETTINGS-01
  - SETTINGS-02
  - SETTINGS-03

must_haves:
  truths:
    - "ContentView tiene jerarquía visual clara: URL input en zona superior prominente, zona de resultado con fondo diferenciado, barra de exportación unificada"
    - "SettingsView muestra indicadores de validación en verde/rojo con texto de ayuda contextual bajo cada campo"
    - "Todos los estados de la app (vacío, extrayendo, éxito, error) tienen tratamiento visual diferenciado y coherente"
    - "La UI funciona en light mode y dark mode sin degradación visual ni textos ilegibles"
    - "Los controles respetan el sistema de espaciado de 8 pt y la jerarquía tipográfica de SF Pro"
  artifacts:
    - path: "ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift"
      provides: "ContentView rediseñada con layout premium y estados visuales"
      contains: "\.padding(24"
    - path: "ExtractorApp/ExtractorApp/ExtractorApp/Views/SettingsView.swift"
      provides: "SettingsView rediseñada con secciones claras y validación visual mejorada"
      contains: "\.font(.headline"
  key_links:
    - from: "ContentView"
      to: "ExtractionViewModel"
      via: "@StateObject vm"
      pattern: "StateObject.*ExtractionViewModel"
    - from: "SettingsView"
      to: "AppStorage pythonPath/scriptPath"
      via: "@AppStorage binding"
      pattern: "AppStorage.*pythonPath|AppStorage.*scriptPath"
---

<objective>
Rediseñar la interfaz visual de ContentView y SettingsView para elevar la calidad estética de la app al nivel premium: jerarquía tipográfica clara, paleta coherente, espaciado generoso y estados visuales diferenciados.

Purpose: La app es funcional pero visualmente básica. Este plan transforma la apariencia sin tocar ninguna lógica de negocio ni el ViewModel — solo CSS equivalente en SwiftUI: colores, espaciado, tipografía, estados visuales.

Output:
- ContentView.swift rediseñada: zona URL prominente, resultado con fondo sutil diferenciado, progreso atractivo, error con contexto visual claro
- SettingsView.swift rediseñada: secciones con cabecera, campos con icono prefijo, validación inline con texto de ayuda, información contextual mejor presentada
</objective>

<execution_context>
@/Users/edefrutos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-python-bridge-y-preferencias/03-CONTEXT.md

@ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift
@ExtractorApp/ExtractorApp/ExtractorApp/Views/SettingsView.swift
@ExtractorApp/ExtractorApp/ExtractorApp/ViewModels/ExtractionViewModel.swift

<interfaces>
<!-- Contratos del ViewModel que ContentView consume. No modificar. -->

ExtractionViewModel (@StateObject):
  @Published var urlString: String
  @Published var outputType: String          // "text" | "html" | "markdown"
  @Published var selectorCSS: String
  @Published var timeout: Int
  @Published var isExtracting: Bool
  @Published var resultContent: String?
  @Published var errorMessage: String?
  @Published var isPythonPathError: Bool
  @Published var contentReady: Bool
  @Published var exportFormat: String        // "markdown" | "html" | "pdf"
  @Published var pageTitle: String?
  var htmlForPreview: String?                // computada
  func extract()
  func export() async

SettingsView AppStorage:
  @AppStorage("pythonPath") var pythonPath: String
  @AppStorage("scriptPath") var scriptPath: String
  FileManager.default.isExecutableFile(atPath:) → Bool (python)
  FileManager.default.fileExists(atPath:)       → Bool (script)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContentView — rediseño visual premium</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift</files>
  <action>
    Reescribir ContentView.swift manteniendo exactamente la misma lógica y bindings al ViewModel, elevando solo la presentación visual. No modificar ExtractionViewModel ni WebPreviewView.

    ESTRUCTURA GENERAL:
    Envolver el VStack principal en un ZStack con fondo Color(.windowBackgroundColor) para que la ventana use el gris Apple nativo. Padding exterior de 24 pt en todos los lados. minWidth 560, minHeight 480 (vs 500/450 anterior — más espacio respira la UI).

    ZONA SUPERIOR — URL INPUT:
    Eliminar el simple TextField plano. Reemplazar por una HStack con fondo RoundedRectangle(cornerRadius: 10) con fill Color(.textBackgroundColor) y stroke Color.secondary.opacity(0.3) de lineWidth 1. Dentro: icono SF "link" en .foregroundStyle(.secondary) + TextField con .font(.body) y .textFieldStyle(.plain). El botón "Extraer" permanece fuera de la cápsula, a la derecha, con .buttonStyle(.borderedProminent) y cornerRadius 8. Espaciado interior de la cápsula: padding horizontal 12, vertical 10.

    SEPARADOR VISUAL (sustituye Divider):
    Entre zonas usar Divider() con .padding(.horizontal, -24) para sangrar al ancho completo de la ventana, dando efecto "panel completo" como en las apps Apple nativas.

    PICKER DE TIPO:
    Mantener .pickerStyle(.segmented). Añadir Label text "Formato:" con .font(.subheadline).foregroundStyle(.secondary) a la izquierda del Picker.

    OPCIONES AVANZADAS:
    DisclosureGroup con .font(.subheadline) para la etiqueta. Los campos interiores siguen el mismo patrón de cápsula que el URL input (cornerRadius 8, fondo textBackgroundColor, stroke secondary 0.2). Los labels de ancho fijo 140 pt (vs 160 anterior).

    ZONA RESULTADO (estado éxito y error):
    Envolver el Group de resultado en una RoundedRectangle(cornerRadius: 12) con fill Color(.controlBackgroundColor) y clipShape. Esto da un fondo sutil diferenciado al área de preview.

    Estado "extrayendo" (APP-02):
    VStack { ProgressView(); Text("Extrayendo contenido…").font(.subheadline).foregroundStyle(.secondary) }. Centrado en el área de resultado. Animación .scaleEffect + .opacity con withAnimation para entrada suave.

    Estado "error" (APP-03):
    HStack(alignment: .top) { Image(systemName: "exclamationmark.triangle.fill").foregroundStyle(.orange).font(.title3); VStack(alignment: .leading, spacing: 2) { Text("Error de extracción").font(.subheadline).bold(); Text(errorMsg).font(.caption).foregroundStyle(.secondary) } }. Si isPythonPathError: añadir Button("Abrir Preferencias") { NSApp.sendAction(Selector(("showPreferencesWindow:")), to: nil, from: nil) }.buttonStyle(.link).font(.caption).

    BARRA DE EXPORTACIÓN:
    Mantener la HStack Picker+Botón Exportar. Añadir .padding(.top, 8) y separar visualmente del preview con Divider al inicio de la sección.

    ACCESIBILIDAD:
    - El TextField URL tiene .accessibilityLabel("URL a extraer")
    - El botón Extraer mantiene su .accessibilityLabel existente
    - La zona de resultado tiene .accessibilityElement(children: .contain)

    DARK MODE:
    Usar exclusivamente colores semánticos del sistema (Color(.windowBackgroundColor), Color(.textBackgroundColor), Color(.controlBackgroundColor), .primary, .secondary) — NUNCA hex hardcodeados. Estos colores invierten automáticamente en dark mode.

    NO modificar: ninguna llamada a vm.extract(), vm.export(), los bindings existentes, WebPreviewView ni su inicialización.
  </action>
  <verify>
    <automated>xcodebuild -project /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp.xcodeproj -scheme ExtractorApp -configuration Debug build 2>&1 | tail -3</automated>
    <automated>grep -n "Color(.windowBackgroundColor)\|Color(.textBackgroundColor)\|Color(.controlBackgroundColor)" /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift | wc -l</automated>
    <automated>grep -c "exclamationmark.triangle.fill" /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift</automated>
  </verify>
  <done>BUILD SUCCEEDED. ContentView usa colores semánticos del sistema (>=3 ocurrencias), incluye el icono de error visual, y compila sin warnings de deprecación.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsView — rediseño visual premium</name>
  <files>ExtractorApp/ExtractorApp/ExtractorApp/Views/SettingsView.swift</files>
  <action>
    Reescribir SettingsView.swift elevando el diseño visual. Mantener exactamente los mismos @AppStorage bindings y la lógica de validación (FileManager checks). No cambiar la lógica, solo la presentación.

    ESTRUCTURA GENERAL:
    Mantener Form con .formStyle(.grouped). Frame width 520 (vs 480 anterior). Añadir un título "Preferencias" con .font(.title2).bold() en la cabecera, sobre el Form.

    SECCIÓN RUTAS — cabecera con icono:
    Section header: Label("Rutas de ejecución", systemImage: "terminal").font(.headline). Esto es idiomático en macOS Settings con .formStyle(.grouped).

    CAMPO PYTHON — diseño mejorado:
    Estructura por campo:
    1. HStack(spacing: 8): Image(systemName: "chevron.right.2").foregroundStyle(.secondary) + VStack(alignment: .leading, spacing: 2) { TextField(...) + Text("Intérprete Python nativo…").font(.caption2).foregroundStyle(.tertiary) } + validationBadge(valid:path:)
    El texto de ayuda bajo el TextField es nuevo: explica brevemente qué poner. Caption2 en tertiary es casi imperceptible pero está ahí para usuarios avanzados.

    CAMPO SCRIPT — mismo patrón con texto de ayuda diferente:
    Texto de ayuda: "Ruta absoluta al fichero extractor_url.py"

    FUNCIÓN validationBadge (reemplaza validationIcon):
    En lugar de solo un icono, mostrar una cápsula pequeña: si valid → Capsule con fill .green.opacity(0.15) + Label("OK", systemImage: "checkmark").font(.caption2).foregroundStyle(.green). Si inválido y path no vacío → Capsule con fill .red.opacity(0.15) + Label("No encontrado", systemImage: "xmark").font(.caption2).foregroundStyle(.red). Si vacío → EmptyView.
    La cápsula es más informativa que el icono solo.

    SECCIÓN INFORMACIÓN — rediseño:
    Section header: Label("Ayuda", systemImage: "info.circle"). Contenido: VStack(alignment: .leading, spacing: 8) con tres Text separados (no un bloque monolítico): 1) "Usar el Python nativo de la arquitectura del Mac (no Rosetta)." 2) "Ejemplo: /Users/usuario/proyectos/extractor-url/.venv/bin/python" en .font(.caption).monospaced().foregroundStyle(.secondary). 3) Si alguna ruta es inválida, mostrar un banner de aviso inline (HStack con icono warning.fill naranja + texto explicativo).

    SECCIÓN BOTÓN DE VERIFICACIÓN (nueva, útil):
    Section { Button("Verificar configuración") { Task { await verifyConfiguration() } }.buttonStyle(.bordered) }
    La función verifyConfiguration() ejecuta process.run() con `python --version` usando pythonPath y almacena el resultado en @State var verificationResult: String? que se muestra en la sección con .font(.caption).monospaced(). Esto es SETTINGS-03 enriquecido.

    ESTADO LOCAL:
    @State private var verificationResult: String? = nil
    @State private var isVerifying: Bool = false
    La función verifyConfiguration() usa Process() directamente (no PythonBridge) para ejecutar [pythonPath, "--version"], lee stdout, asigna verificationResult en MainActor.

    DARK MODE:
    Misma regla que ContentView: solo colores semánticos del sistema o modificadores .opacity(). Los capsule badges usan .green.opacity(0.15) y .red.opacity(0.15) que son visibles en ambos modos.

    NO modificar: los bindings @AppStorage existentes, la lógica de validación con FileManager.
  </action>
  <verify>
    <automated>xcodebuild -project /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp.xcodeproj -scheme ExtractorApp -configuration Debug build 2>&1 | tail -3</automated>
    <automated>grep -n "Capsule\|validationBadge\|verifyConfiguration\|verificationResult" /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp/ExtractorApp/Views/SettingsView.swift</automated>
    <automated>grep -c "AppStorage" /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp/ExtractorApp/Views/SettingsView.swift</automated>
  </verify>
  <done>BUILD SUCCEEDED. SettingsView contiene Capsule badges, función verifyConfiguration, y conserva los 2 bindings @AppStorage originales. xcodebuild sin errores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → ViewModel | ContentView solo lee y bindea @Published; nunca escribe estado directamente |
| SettingsView → Process | verifyConfiguration() lanza `python --version` con ruta configurada por el usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | SettingsView.verifyConfiguration Process | mitigate | Ejecutar solo `--version` (sin args controlados por el usuario en esta llamada); la ruta pythonPath ya validada por FileManager.isExecutableFile antes de lanzar |
| T-03-04-02 | Information Disclosure | verificationResult mostrado en UI | accept | Solo muestra la salida de `python --version` — sin datos sensibles |
| T-03-04-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes — solo modifica ficheros Swift UI |
</threat_model>

<verification>
Tras completar ambas tareas:

```bash
# Build limpio
xcodebuild -project /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp.xcodeproj \
  -scheme ExtractorApp -configuration Debug build 2>&1 | tail -5

# ContentView usa colores semánticos del sistema
grep -c "Color(\.window\|Color(\.text\|Color(\.control" \
  ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift

# SettingsView conserva @AppStorage bindings originales
grep -c "AppStorage" \
  ExtractorApp/ExtractorApp/ExtractorApp/Views/SettingsView.swift

# Verificar ausencia de colores hex hardcodeados
grep -n "#[0-9A-Fa-f]\{6\}" \
  ExtractorApp/ExtractorApp/ExtractorApp/ContentView.swift \
  ExtractorApp/ExtractorApp/ExtractorApp/Views/SettingsView.swift
```
</verification>

<success_criteria>
- BUILD SUCCEEDED en xcodebuild Debug sin errores ni warnings de deprecación
- ContentView: zona URL con cápsula visual, zona resultado con fondo diferenciado, estado error con icono triangle warning, dark mode con colores semánticos
- SettingsView: validation badges como cápsulas (no solo iconos), textos de ayuda bajo cada campo, botón "Verificar configuración" funcional, sección Ayuda con ejemplo monospaced
- Ningún color hex hardcodeado en ninguno de los dos ficheros
- La app arranca y responde a Cmd+, (Preferencias) sin crashear
</success_criteria>

<output>
Crear `.planning/phases/03-python-bridge-y-preferencias/03-04-SUMMARY.md` cuando este plan esté completo.
</output>
