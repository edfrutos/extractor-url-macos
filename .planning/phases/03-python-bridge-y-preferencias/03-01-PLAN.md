---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj
  - ExtractorApp/ExtractorApp/ExtractorApp.entitlements
  - ExtractorApp/ExtractorApp/Models/ExtractionResult.swift
  - ExtractorApp/ExtractorApp/Models/ExtractionError.swift
  - ExtractorApp/ExtractorApp/ContentView.swift
autonomous: false
requirements:
  - APP-05
  - BRIDGE-02
  - BRIDGE-04

must_haves:
  truths:
    - "El proyecto Xcode compila sin errores con deployment target macOS 13.0"
    - "El entitlement app-sandbox está explícitamente a false en el fichero .entitlements"
    - "ExtractionResult decodifica correctamente el JSON del CLI Python (campos snake_case mapeados con CodingKeys)"
    - "ExtractionError cubre los cinco casos de error tipados del bridge"
  artifacts:
    - path: "ExtractorApp/ExtractorApp.xcodeproj/project.pbxproj"
      provides: "Proyecto Xcode con build settings APP-05"
    - path: "ExtractorApp/ExtractorApp/ExtractorApp.entitlements"
      provides: "Entitlement app-sandbox = false"
      contains: "com.apple.security.app-sandbox"
    - path: "ExtractorApp/ExtractorApp/Models/ExtractionResult.swift"
      provides: "Modelo Codable que mapea el JSON del CLI"
      exports: ["ExtractionResult"]
    - path: "ExtractorApp/ExtractorApp/Models/ExtractionError.swift"
      provides: "Enum tipado con los cinco casos de error del bridge"
      exports: ["ExtractionError"]
  key_links:
    - from: "ExtractionResult"
      to: "JSON CLI output"
      via: "CodingKeys mapping"
      pattern: "output_type.*outputType|char_count.*charCount|error_message.*errorMessage"
    - from: "ExtractorApp.entitlements"
      to: "Xcode build settings"
      via: "CODE_SIGN_ENTITLEMENTS"
      pattern: "CODE_SIGN_ENTITLEMENTS"
---

<objective>
Crear el proyecto Xcode base de la app macOS y los modelos de datos Swift que el bridge necesita para decodificar las respuestas del CLI Python.

Purpose: Este plan establece la estructura del proyecto y los contratos de datos. Sin el proyecto Xcode configurado correctamente (App Sandbox OFF, Hardened Runtime ON) Foundation.Process() no puede ejecutarse. Sin ExtractionResult y ExtractionError, PythonBridge no tiene tipos contra los que compilar.

Output:
- Proyecto Xcode en ExtractorApp/ con deployment target macOS 13.0, ARCHS_STANDARD, Hardened Runtime ON, App Sandbox OFF
- ExtractorApp.entitlements con com.apple.security.app-sandbox = false
- ExtractionResult.swift: struct Codable con CodingKeys para campos snake_case del JSON
- ExtractionError.swift: enum con cinco casos tipados
- ContentView.swift: placeholder vacío (se implementa en fase 4)
</objective>

<execution_context>
@/Users/edefrutos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-python-bridge-y-preferencias/03-CONTEXT.md
@.planning/phases/03-python-bridge-y-preferencias/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Crear proyecto Xcode via GUI</name>
  <read_first>
    .planning/phases/03-python-bridge-y-preferencias/03-RESEARCH.md (sección 6: Xcode Project — Creación, sección 5: Entitlements)
    .planning/phases/03-python-bridge-y-preferencias/03-CONTEXT.md (sección Xcode Project Setup)
  </read_first>
  <what-built>
    Esta tarea requiere abrir Xcode GUI para crear el proyecto base. Claude no puede crear un .xcodeproj funcional sin Xcode.
  </what-built>
  <how-to-verify>
    1. Abrir Xcode → File → New → Project → macOS → App
    2. Configurar:
       - Product Name: ExtractorApp
       - Team: Personal Team (o dejar vacío)
       - Organization Identifier: com.edefrutos
       - Bundle Identifier resultante: com.edefrutos.extractor-url (ajustar si Xcode no permite guión: com.edefrutos.extractorurl)
       - Interface: SwiftUI
       - Language: Swift
       - Desmarcar "Include Tests" (los tests de la app se difieren)
    3. Guardar en: raíz del repo como ExtractorApp/ (sibling de extractor_url.py y core.py)
    4. En Project Navigator → seleccionar target ExtractorApp → Build Settings:
       - MACOSX_DEPLOYMENT_TARGET = 13.0
       - ARCHS = $(ARCHS_STANDARD)
       - SWIFT_VERSION = 5.9
       - ENABLE_HARDENED_RUNTIME = YES
       - CODE_SIGN_INJECT_BASE_ENTITLEMENTS = NO
    5. Añadir fichero de entitlements: File → New → File → Property List → nombre ExtractorApp.entitlements
       - Mover al grupo ExtractorApp/ dentro del proyecto
       - En Build Settings → CODE_SIGN_ENTITLEMENTS = ExtractorApp/ExtractorApp.entitlements
    6. Añadir grupos en Project Navigator: Models/, Services/, Views/ dentro de ExtractorApp/
    7. Verificar que el proyecto compila: Product → Build (Cmd+B) — debe completar sin errores
    8. Escribir "listo" para continuar
  </how-to-verify>
  <resume-signal>Escribe "listo" cuando el proyecto compile con Cmd+B sin errores y los grupos estén creados</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Crear ExtractorApp.entitlements y modelos Swift</name>
  <read_first>
    ExtractorApp/ExtractorApp/ExtractorApp.entitlements (leer si ya existe del paso anterior)
    .planning/phases/03-python-bridge-y-preferencias/03-RESEARCH.md (sección 5: Entitlements, sección 8: ExtractionResult)
    .planning/phases/03-python-bridge-y-preferencias/03-CONTEXT.md (sección ExtractionResult y Error Handling)
    extractor_url.py (líneas 280-298: JSON output del CLI)
  </read_first>
  <action>
    Crear o reemplazar el fichero de entitlements y los dos modelos Swift. No crear código de UI todavía.

    ENTITLEMENTS — fichero ExtractorApp/ExtractorApp/ExtractorApp.entitlements:
    Fichero plist XML con una única clave activa: com.apple.security.app-sandbox con valor false (boolean). Incluir como comentario XML la clave allow-unsigned-executable-memory desactivada para determinarla empíricamente al ejecutar el bridge. No añadir ningún otro entitlement.

    EXTRACTION RESULT — fichero ExtractorApp/ExtractorApp/Models/ExtractionResult.swift:
    - Cabecera: import Foundation
    - struct ExtractionResult: Codable con las propiedades:
      - status: String (valores posibles: "success" | "error")
      - url: String
      - selector: String? (nil en error)
      - outputType: String? (nil en error; clave JSON: "output_type")
      - charCount: Int? (nil en error; clave JSON: "char_count")
      - content: String? (nil en error)
      - errorMessage: String? (nil en success; clave JSON: "error_message")
    - enum CodingKeys: String, CodingKey con los casos: status, url, selector, content, outputType = "output_type", charCount = "char_count", errorMessage = "error_message"
    - Propiedad computada: var isSuccess: Bool { status == "success" }

    EXTRACTION ERROR — fichero ExtractorApp/ExtractorApp/Models/ExtractionError.swift:
    - Cabecera: import Foundation
    - enum ExtractionError: Error, LocalizedError con los casos:
      - pythonNotFound(path: String) — el ejecutable configurado no existe o no tiene permisos x
      - processLaunchFailed(underlying: Error) — process.run() lanzó excepción
      - extractionFailed(message: String) — CLI devolvió status: "error"
      - jsonDecodeFailed(underlying: Error) — stdout no es JSON válido
      - emptyOutput — stdout vacío tras terminación normal
    - Propiedad var errorDescription: String? implementando LocalizedError con mensajes en español para cada caso

    CONTENT VIEW PLACEHOLDER — fichero ExtractorApp/ExtractorApp/ContentView.swift:
    - Struct ContentView: View con body que muestra Text("Fase 3 — bridge en construcción")
    - Ninguna lógica adicional (se implementa en fase 4)

    IMPORTANTE: No poner bloques de código con triple backtick dentro de los ficheros Swift. Escribir código Swift directamente.
  </action>
  <verify>
    <automated>
      find /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp -name "*.swift" -o -name "*.entitlements" 2>/dev/null | sort
    </automated>
    <automated>
      grep -c "com.apple.security.app-sandbox" /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp/ExtractorApp.entitlements
    </automated>
    <automated>
      grep -n "output_type\|char_count\|error_message" /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp/Models/ExtractionResult.swift
    </automated>
    <automated>
      grep -n "pythonNotFound\|processLaunchFailed\|extractionFailed\|jsonDecodeFailed\|emptyOutput" /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp/ExtractorApp/Models/ExtractionError.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - ExtractorApp/ExtractorApp/ExtractorApp.entitlements existe y contiene com.apple.security.app-sandbox con valor false
    - ExtractorApp/ExtractorApp/Models/ExtractionResult.swift existe, define struct ExtractionResult: Codable con CodingKeys que mapea output_type, char_count, error_message
    - ExtractorApp/ExtractorApp/Models/ExtractionError.swift existe con los cinco casos: pythonNotFound, processLaunchFailed, extractionFailed, jsonDecodeFailed, emptyOutput
    - ExtractorApp/ExtractorApp/ContentView.swift existe como placeholder
    - xcodebuild -project ExtractorApp/ExtractorApp.xcodeproj -scheme ExtractorApp -configuration Debug build compila sin errores
  </acceptance_criteria>
  <done>Los tres ficheros Swift existen en las rutas correctas, los CodingKeys mapean los campos snake_case del CLI Python, y el proyecto compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON stdout → JSONDecoder | Datos del proceso hijo cruzando hacia el modelo Swift |
| Fichero .entitlements → código firmado | Permisos declarados al sistema operativo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ExtractionResult CodingKeys | mitigate | CodingKeys explícitos: cualquier campo inesperado del JSON es ignorado por el decoder; nunca se usa try? sin inspección |
| T-03-02 | Information Disclosure | ExtractorApp.entitlements | accept | App Sandbox OFF es intencional para herramienta personal sin App Store; documentado en CONTEXT.md |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes — solo crea ficheros Swift y plist |
</threat_model>

<verification>
Tras completar este plan:

```bash
# Verificar entitlement
grep -A1 "app-sandbox" ExtractorApp/ExtractorApp/ExtractorApp.entitlements

# Verificar CodingKeys presentes
grep "output_type\|char_count\|error_message" ExtractorApp/ExtractorApp/Models/ExtractionResult.swift

# Verificar cinco casos de error
grep -c "case python\|case process\|case extraction\|case json\|case empty" ExtractorApp/ExtractorApp/Models/ExtractionError.swift

# Compilación limpia
xcodebuild -project ExtractorApp/ExtractorApp.xcodeproj -scheme ExtractorApp -configuration Debug build 2>&1 | tail -5
```
</verification>

<success_criteria>
- El proyecto Xcode existe en ExtractorApp/ y compila con Cmd+B / xcodebuild sin errores
- ExtractorApp.entitlements declara app-sandbox = false
- ExtractionResult.swift: struct Codable con CodingKeys que mapean output_type → outputType, char_count → charCount, error_message → errorMessage
- ExtractionError.swift: enum con cinco casos tipados (pythonNotFound, processLaunchFailed, extractionFailed, jsonDecodeFailed, emptyOutput)
- Build settings: MACOSX_DEPLOYMENT_TARGET = 13.0, ENABLE_HARDENED_RUNTIME = YES, CODE_SIGN_ENTITLEMENTS apunta al .entitlements
</success_criteria>

<output>
Crear `.planning/phases/03-python-bridge-y-preferencias/03-01-SUMMARY.md` cuando este plan esté completo.
</output>
