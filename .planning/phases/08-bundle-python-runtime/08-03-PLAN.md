# [08-03] Phase 08: Bundle Python Runtime — Plan 03: Validación end-to-end

**Wave**: 2 | **Depends on**: 08-01, 08-02
**Requirements**: BUNDLE-01, BUNDLE-02, BUNDLE-03
**Goal**: Crear `scripts/verify-bundle.sh` que valida los tres requisitos BUNDLE-XX contra el `.app` compilado, crear `BundlePathTests.swift` con XCTest que verifica los métodos de bundle path desde Swift, ejecutar un build Release completo y confirmar con un checkpoint humano que una URL real se extrae usando el Python bundleado (sin Python del sistema).

---

## Context

Los planes 08-01 y 08-02 crean la infraestructura de bundle. Este plan verifica que todo funciona de extremo a extremo: el binario Python es universal (BUNDLE-01), los scripts están presentes (BUNDLE-02) y las deps son importables sin red ni pip del sistema (BUNDLE-03).

La validación tiene dos capas:

1. **Shell (`verify-bundle.sh`):** Valida el bundle directamente sobre el `.app` compilado. Portátil, no requiere Xcode ni los tests de Swift. Útil para CI y para verificar manualmente después de un Archive.

2. **XCTest (`BundlePathTests.swift`):** Verifica que los métodos `bundledPythonPath()`, `bundledScriptPath()` y `bundledVendoredLibPath()` de `PythonBridge` devuelven rutas válidas dentro del bundle de test. Se integra en la suite existente `ExtractorAppTests`.

3. **Checkpoint humano:** Verificación funcional real — el usuario desactiva el Python del sistema (o lo renombra temporalmente) y confirma que la app extrae una URL correctamente.

---

## Pre-conditions

- [ ] Plan 08-01 completado: `scripts/bundle-python.sh` existe y pasa `bash -n`
- [ ] Plan 08-02 completado: Run Script Build Phase configurada en Xcode, `PythonBridge.bundledPythonPath()` compilado sin errores
- [ ] Build exitoso (Cmd+B) con el runtime Python generado en el bundle

---

## Tasks

### Task 1: Crear `scripts/verify-bundle.sh`

**File**: `scripts/verify-bundle.sh`
**Action**: Create

Script de validación que recibe la ruta al `.app` como primer argumento. Si no se pasa argumento, busca el `.app` en `~/Library/Developer/Xcode/DerivedData/` automáticamente.

```
#!/usr/bin/env bash
# verify-bundle.sh — Valida BUNDLE-01, BUNDLE-02, BUNDLE-03
# Uso: ./scripts/verify-bundle.sh [/path/to/ExtractorApp.app]
#
# Sin argumento: busca el .app más reciente en DerivedData de Xcode.
set -euo pipefail

PASS=0
FAIL=0

ok()   { echo "  OK : $*"; ((PASS++)) || true; }
fail() { echo "  FAIL: $*" >&2; ((FAIL++)) || true; }

# ── Localizar el .app ─────────────────────────────────────────────────────────
if [[ $# -ge 1 ]]; then
  APP="$1"
else
  # Buscar en DerivedData el build más reciente de ExtractorApp
  APP=$(find ~/Library/Developer/Xcode/DerivedData -name "ExtractorApp.app" \
        -not -path "*/Index.noindex/*" \
        2>/dev/null | head -1)
  if [[ -z "$APP" ]]; then
    echo "ERROR: No se encontró ExtractorApp.app. Pasa la ruta como argumento." >&2
    echo "  Uso: $0 /path/to/ExtractorApp.app" >&2
    exit 1
  fi
fi

echo "Validando bundle: $APP"
echo ""

RESOURCES="${APP}/Contents/Resources"

# ── BUNDLE-01: Universal Python binary ───────────────────────────────────────
echo "=== BUNDLE-01: Intérprete Python universal (arm64+x86_64) ==="
PYTHON="${RESOURCES}/python/bin/python3.13"

if [[ -f "${PYTHON}" ]]; then
  if [[ -x "${PYTHON}" ]]; then
    ok "python3.13 existe y es ejecutable"
  else
    fail "python3.13 existe pero no es ejecutable"
  fi

  ARCHS=$(lipo -archs "${PYTHON}" 2>&1)
  if echo "${ARCHS}" | grep -q "x86_64"; then
    ok "arquitectura x86_64 presente (archs: ${ARCHS})"
  else
    fail "arquitectura x86_64 AUSENTE (archs: ${ARCHS})"
  fi

  if echo "${ARCHS}" | grep -q "arm64"; then
    ok "arquitectura arm64 presente"
  else
    fail "arquitectura arm64 AUSENTE"
  fi

  # Verificar firma de código
  if codesign --verify --strict "${PYTHON}" 2>/dev/null; then
    ok "python3.13 tiene firma de código válida"
  else
    fail "python3.13 no tiene firma de código válida (puede fallar en distribución)"
  fi

  # Verificar versión
  VERSION=$("${PYTHON}" --version 2>&1)
  ok "versión: ${VERSION}"
else
  fail "python3.13 NO encontrado en ${PYTHON}"
fi

echo ""

# ── BUNDLE-02: Scripts en Resources/scripts/ ──────────────────────────────────
echo "=== BUNDLE-02: Scripts Python en Resources/scripts/ ==="
SCRIPTS_DIR="${RESOURCES}/scripts"

for script in extractor_url.py core.py; do
  if [[ -f "${SCRIPTS_DIR}/${script}" ]]; then
    ok "${script} presente en scripts/"
  else
    fail "${script} NO encontrado en ${SCRIPTS_DIR}/"
  fi
done

echo ""

# ── BUNDLE-03: Deps vendorizadas importables ──────────────────────────────────
echo "=== BUNDLE-03: Deps Python vendorizadas importables ==="
VENDORED_LIB="${RESOURCES}/python/lib/python-packages"

if [[ -d "${VENDORED_LIB}" ]]; then
  ok "directorio python-packages existe"
else
  fail "directorio python-packages NO encontrado en ${VENDORED_LIB}"
fi

if [[ -x "${PYTHON}" ]]; then
  # Test de importabilidad con PYTHONPATH apuntando a las deps vendorizadas
  IMPORT_TEST=$(PYTHONPATH="${VENDORED_LIB}" "${PYTHON}" -c "
import sys
failed = []
for pkg in ['requests', 'bs4', 'lxml', 'markdownify', 'trafilatura']:
    try:
        m = __import__(pkg)
        ver = getattr(m, '__version__', 'unknown')
        print(f'OK: {pkg} {ver}')
    except ImportError as e:
        failed.append(f'{pkg}: {e}')
        print(f'FAIL: {pkg} — {e}', file=sys.stderr)
if failed:
    sys.exit(1)
" 2>&1) || true

  if echo "${IMPORT_TEST}" | grep -q "^FAIL:"; then
    fail "algunas deps no son importables:"
    echo "${IMPORT_TEST}" | grep "^FAIL:" | sed 's/^/    /' >&2
  else
    echo "${IMPORT_TEST}" | sed 's/^OK:/    ok :/'
    ((PASS += 5)) || true
  fi

  # Test funcional: extrae example.com con el bundle completo
  echo ""
  echo "=== Test funcional: extraer https://example.com ==="
  SCRIPT="${SCRIPTS_DIR}/extractor_url.py"
  if [[ -f "${SCRIPT}" ]]; then
    RESULT=$(PYTHONPATH="${VENDORED_LIB}" "${PYTHON}" "${SCRIPT}" \
      https://example.com --type text --json 2>&1) || true
    if echo "${RESULT}" | python3 -c "import sys,json; d=json.load(sys.stdin); sys.exit(0 if d.get('success') else 1)" 2>/dev/null; then
      ok "extracción de example.com devolvió JSON con success=true"
    else
      fail "extracción de example.com falló o no devolvió JSON válido"
      echo "  Output: ${RESULT:0:200}" >&2
    fi
  else
    fail "Script principal ${SCRIPT} no encontrado — omitiendo test funcional"
  fi
fi

# ── Resumen ───────────────────────────────────────────────────────────────────
echo ""
echo "═══════════════════════════════════════════════════"
echo "  Resultado: ${PASS} OK  |  ${FAIL} FAIL"
echo "═══════════════════════════════════════════════════"

if [[ ${FAIL} -gt 0 ]]; then
  echo "  BUNDLE VALIDATION: FAILED" >&2
  exit 1
else
  echo "  BUNDLE VALIDATION: PASS — BUNDLE-01, BUNDLE-02, BUNDLE-03 satisfechos"
  exit 0
fi
```

Tras crear el archivo: `chmod +x scripts/verify-bundle.sh`

### Task 2: Crear `BundlePathTests.swift`

**File**: `ExtractorApp/ExtractorApp/ExtractorAppTests/BundlePathTests.swift`
**Action**: Create

XCTest que verifica los métodos estáticos de bundle path en `PythonBridge`. Estos tests se ejecutan desde el bundle de tests de Xcode, donde `Bundle.main.resourcePath` apunta a los recursos del test runner — por eso verifican la lógica de construcción de rutas, no la existencia física de los archivos (que solo existe en el `.app` de Release).

```swift
import XCTest
@testable import ExtractorApp

/// Tests para los métodos de resolución de rutas bundled en PythonBridge.
///
/// NOTA: Estos tests verifican la lógica de construcción de rutas.
/// En el contexto del test runner, `Bundle.main.resourcePath` apunta
/// a los recursos del host de tests, no al bundle de producción.
/// Los tests de existencia real se hacen con verify-bundle.sh sobre
/// el .app compilado.
final class BundlePathTests: XCTestCase {

    // MARK: - bundledPythonPath

    func testBundledPythonPathReturnsNilWhenNotPresent() {
        // En el entorno de tests, el binario bundled no existe.
        // bundledPythonPath() debe devolver nil (no crashear).
        // Este test documenta el contrato: nil = no disponible en este contexto.
        let path = PythonBridge.bundledPythonPath()
        // Si el test corre contra el .app real, el path existirá.
        // Si corre en el test runner normal, será nil.
        // En ambos casos, el método no debe lanzar excepción.
        if let path = path {
            XCTAssertTrue(path.hasSuffix("/python/bin/python3.13"),
                         "La ruta bundled debe terminar en /python/bin/python3.13, got: \(path)")
            XCTAssertTrue(path.contains("Resources"),
                         "La ruta bundled debe estar dentro de Resources")
        }
        // nil es un resultado válido (bundle no compilado todavía)
    }

    func testBundledPythonPathHasCorrectStructure() {
        // Verificar que la ruta construida tiene la estructura correcta.
        // Forzamos la construcción usando Bundle.main.resourcePath directamente.
        guard let resourcePath = Bundle.main.resourcePath else {
            XCTFail("Bundle.main.resourcePath no disponible en este entorno")
            return
        }
        let expectedSuffix = "/python/bin/python3.13"
        let expectedPath = resourcePath + expectedSuffix

        // bundledPythonPath() solo difiere en que verifica isExecutableFile.
        // Verificamos que la estructura de la ruta es correcta:
        XCTAssertTrue(expectedPath.hasSuffix(expectedSuffix))
        XCTAssertFalse(expectedPath.contains("//"),
                      "La ruta no debe tener barras dobles")
    }

    // MARK: - bundledScriptPath

    func testBundledScriptPathReturnsNilOrValidSuffix() {
        let path = PythonBridge.bundledScriptPath()
        if let path = path {
            XCTAssertTrue(path.hasSuffix("/scripts/extractor_url.py"),
                         "La ruta del script debe terminar en /scripts/extractor_url.py")
            XCTAssertTrue(path.contains("Resources"),
                         "El script debe estar dentro de Resources")
        }
        // nil es válido: archivo no existe en el bundle de tests
    }

    func testBundledScriptPathStructure() {
        guard let resourcePath = Bundle.main.resourcePath else { return }
        let expectedPath = resourcePath + "/scripts/extractor_url.py"
        XCTAssertTrue(expectedPath.contains("/scripts/extractor_url.py"))
        XCTAssertFalse(expectedPath.contains("//"))
    }

    // MARK: - bundledVendoredLibPath

    func testBundledVendoredLibPathNeverNil() {
        // bundledVendoredLibPath() siempre devuelve una ruta (no valida existencia).
        // Devuelve nil solo si Bundle.main.resourcePath falla (no ocurre en producción).
        let path = PythonBridge.bundledVendoredLibPath()
        // En el test runner con Bundle.main.resourcePath disponible, no debe ser nil.
        if Bundle.main.resourcePath != nil {
            XCTAssertNotNil(path,
                           "bundledVendoredLibPath() debe devolver ruta cuando resourcePath está disponible")
        }
    }

    func testBundledVendoredLibPathHasCorrectStructure() {
        guard let path = PythonBridge.bundledVendoredLibPath() else { return }
        XCTAssertTrue(path.hasSuffix("/python/lib/python-packages"),
                     "La ruta de deps debe terminar en /python/lib/python-packages")
        XCTAssertTrue(path.contains("Resources"))
        XCTAssertFalse(path.contains("//"))
    }

    // MARK: - Consistencia entre métodos

    func testAllBundledPathsShareSameResourceBase() {
        // Todos los paths bundled deben derivar del mismo resourcePath base.
        guard let resourcePath = Bundle.main.resourcePath else { return }

        let vendoredPath = PythonBridge.bundledVendoredLibPath()
        XCTAssertEqual(vendoredPath, resourcePath + "/python/lib/python-packages",
                      "La ruta vendored debe construirse sobre resourcePath")
    }
}
```

**Nota sobre la suite de tests existente:** El directorio `ExtractorApp/ExtractorApp/ExtractorAppTests/` ya existe (confirmado en el git status). Añadir este archivo sin modificar los tests existentes (`PythonBridgeTests.swift`, `SettingsViewModelTests.swift`).

### Task 3: Build Release y validación end-to-end (checkpoint humano)

**Action**: Verificación manual

**Paso 1 — Build Release:**

En Xcode, seleccionar el scheme "ExtractorApp" y cambiar a configuración Release:
- Product → Scheme → Edit Scheme → Run → Build Configuration: Release
- Cmd+B (o Product → Build)

Alternativamente desde terminal (ajustar `DEVELOPMENT_TEAM` si es necesario):
```bash
cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp
xcodebuild -scheme ExtractorApp -configuration Release \
  CODE_SIGN_IDENTITY="-" \
  build 2>&1 | tail -30
```

**Paso 2 — Ejecutar verify-bundle.sh:**

Localizar el `.app` compilado y ejecutar el script de validación:
```bash
cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url

# Opción A: dejar que verify-bundle.sh lo localice automáticamente
./scripts/verify-bundle.sh

# Opción B: ruta explícita desde DerivedData
APP=$(find ~/Library/Developer/Xcode/DerivedData -name "ExtractorApp.app" \
      -not -path "*/Index.noindex/*" 2>/dev/null | head -1)
./scripts/verify-bundle.sh "$APP"
```

El output esperado:
```
Validando bundle: .../ExtractorApp.app
=== BUNDLE-01: ...
  ok : python3.13 existe y es ejecutable
  ok : arquitectura x86_64 presente (archs: x86_64 arm64)
  ok : arquitectura arm64 presente
  ok : python3.13 tiene firma de código válida
  ok : versión: Python 3.13.14

=== BUNDLE-02: ...
  ok : extractor_url.py presente en scripts/
  ok : core.py presente en scripts/

=== BUNDLE-03: ...
  ok : directorio python-packages existe
  ok : requests 2.34.2
  ok : bs4 4.15.0
  ok : lxml 6.1.1
  [...]
  ok : extracción de example.com devolvió JSON con success=true

═══════════════════════════════════════════════════
  Resultado: 14 OK  |  0 FAIL
═══════════════════════════════════════════════════
  BUNDLE VALIDATION: PASS
```

**Paso 3 — Ejecutar XCTest:**

```bash
cd /Volumes/ESSAGER/__01.-Proyectos/__Herramientas_Desktop/extractor-url/ExtractorApp
xcodebuild test -scheme ExtractorApp -destination "platform=macOS" \
  -only-testing:ExtractorAppTests/BundlePathTests 2>&1 | tail -20
```

**Paso 4 — Checkpoint humano (verificación funcional):**

Abrir el `.app` compilado (doble clic o `open "$APP"`). Con la app abierta:

1. En SettingsView, las rutas de Python y script todavía deben estar configuradas con las rutas del sistema (la Fase 9 cambiará esto). Si no están configuradas, configurarlas manualmente apuntando al bundle: `$APP/Contents/Resources/python/bin/python3.13` y `$APP/Contents/Resources/scripts/extractor_url.py`.
2. Introducir `https://example.com` en el campo de URL.
3. Seleccionar tipo "Texto" y pulsar Extraer.
4. Verificar que el resultado muestra el contenido de example.com (texto del dominio de ejemplo de IANA).
5. Confirmar que en el log de actividad no aparece ningún error de "python not found" ni ImportError.

---

## Verification

- [ ] `scripts/verify-bundle.sh` existe y tiene permisos de ejecución: `ls -la scripts/verify-bundle.sh` muestra `-rwxr-xr-x`
- [ ] Sintaxis válida: `bash -n scripts/verify-bundle.sh` sin errores
- [ ] `BundlePathTests.swift` compila sin errores (Cmd+B con tests target)
- [ ] `xcodebuild test -only-testing:ExtractorAppTests/BundlePathTests` pasa todos los tests
- [ ] Build Release exitoso: `xcodebuild -configuration Release build` termina con `BUILD SUCCEEDED`
- [ ] `./scripts/verify-bundle.sh` termina con `BUNDLE VALIDATION: PASS`
- [ ] BUNDLE-01: `lipo -archs .../python/bin/python3.13` devuelve `x86_64 arm64`
- [ ] BUNDLE-02: `ls .../Contents/Resources/scripts/` muestra `extractor_url.py` y `core.py`
- [ ] BUNDLE-03: `PYTHONPATH=.../python/lib/python-packages .../python3.13 -c "import requests, lxml, trafilatura"` termina con exit 0
- [ ] Checkpoint humano superado: la app extrae `https://example.com` mostrando contenido real

---

## Commit message

`feat(08-03): añade verify-bundle.sh, BundlePathTests.swift y validación end-to-end del bundle Python`
